{
  "private": true,
  "name": "deducto-puzzle",
  "version": "0.0.0",
  "description": "Deducto - a daily deduction puzzle for Reddit, built on Devvit Web (interactive post + Express server).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:bank": "tsx scripts/build-bank.ts 160",
    "check:solvability": "tsx scripts/check-solvability.ts",
    "build:client": "cd src/client && vite build",
    "build:server": "cd src/server && vite build",
    "build": "npm run build:bank && npm run build:client && npm run build:server",
    "dev:client": "cd src/client && vite build --watch",
    "dev:server": "cd src/server && vite build --watch",
    "dev:devvit": "devvit playtest",
    "dev": "concurrently -k -p \"[{name}]\" -n \"CLIENT,SERVER,DEVVIT\" -c \"cyan,green,magenta\" \"npm run dev:client\" \"npm run dev:server\" \"npm run dev:devvit\"",
    "install:all": "devvit install deducto_puzzle_dev && devvit install deducto_puzzle",
    "deploy": "npm run build && devvit upload && npm run install:all",
    "launch": "npm run build && devvit upload && devvit publish && npm run install:all",
    "login": "devvit login",
    "test": "tsx src/server/engine.test.ts",
    "test:routes": "tsx --tsconfig src/server/testing/tsconfig.json src/server/routes.test.ts",
    "type-check": "tsc --noEmit",
    "check:fixture": "tsc -p plans/assets/proto/12-fonts/tsconfig.json",
    "check:layout": "bash plans/assets/proto/12-fonts/fast.sh"
  },
  "dependencies": {
    "@devvit/web": "0.13.11",
    "express": "5.1.0"
  },
  "devDependencies": {
    "@types/express": "5.0.1",
    "@types/node": "^22.12.0",
    "concurrently": "9.1.0",
    "devvit": "0.13.11",
    "tsx": "4.23.0",
    "typescript": "5.8.3",
    "vite": "7.3.5"
  }
}
