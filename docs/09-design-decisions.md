# Design decisions log

Decisions from prototype playtests (ported into the webview client [src/client/](../src/client/)). When there's a dispute over "how it should work" - look here first.

| # | Decision | Reason |
|---|---|---|
| 1 | Board - "single board" (suspects x categories, candidate chips in cells) instead of 3 tabs | The deduction chain jumps between categories on almost every step - tabs break the context; you can't see the whole state at once |
| 2 | "Cross-out" model: click = cross out a candidate | Playtest: the player naturally thinks in exclusions ("it's not him") rather than assignments |
| 3 | Check counts the single remaining candidate as the answer | Playtest 1: the player crossed everything out correctly and got "0/12, not enough deductions" - perceived it as a bug |
| 4 | Full auto-cascade (auto-checkmark cells + columns) - **rejected** | Playtest 2: the board "lives its own life", the player loses the cause-and-effect link |
| 6 | Live clue status (🟢 satisfied / 🔴 violated / gray) | Player's suggestion. Reinforces clarity and gives instant feedback about a contradiction; computed from the player's marks, not from the solution - no spoilers |
| 7 | Drag-and-drop of values into cells - **deferred** | Deduction in this genre is mostly "NOT" statements, drag can't express them (you can't "drag NOT-pizza"); conflicts with scrolling on mobile and in the Reddit webview. Return at the polish stage as an extra gesture for positive assignments |
| 8 | "Active clue" (panel + Prev/Next + selection mode) - **removed** | Playtest 3: the concept is unclear and confusing. The clue list with live statuses fully replaces it |
| 9 | All automation removed - the board is fully manual | Playtest 3: "less automation was clearer", "let nothing happen automatically". Even optional toggles and local auto-cross-out are removed. `yes` no longer crosses out the row/column - only by hand. Crossing out dims candidates, so the remaining answer is visible without indicators; `effectiveValue` (read-only) keeps "crossed everything out = solved" |
| 10 | Board highlight/outline on hovering a clue - **removed** | Playtest 3: confusing. Clues and the board are visually independent; the player keeps the link in their head (that's what deduction is). A hint marks only its own clue in the list (in yellow), it doesn't touch the board |
| 11 | Unified "post" metaphor: everyone posted exactly once at their own time. All clues are about "posted" ("was online / had business / found / touched" removed) | Audit: 5 different verbs for one relation threw people off on first read. "Was not online at 15:00" read ambiguously (not at that moment / left before 15:00 - interval vs instant). "Posted" is an instant event: both ordering ("earlier") and exclusion ("not at 15:00") are unambiguous |
| 12 | Official solution chain switched to a path **without "proof by contradiction"** | The former step "John's item" was solved by trial with a contradiction. The order "first Blue=15:00 ⇒ spoon@15:00, then clue 9 places scroll@09/keyboard@12" makes the whole puzzle derivable by direct elimination - every move is certain without hypotheses |
| 13 | Clue 8 (Paul ≠ Purple) redundancy confirmed | Brute-force: the solution is unique without it too; the other 9 clues are load-bearing. Keep it as an optional crutch or drop it for the minimal set |
| 14 | `yes` confirmation removed from the board - pure toggle (click = cross out/restore, two states) | Playtest 4: the green confirmation badge collapsed the cell and hid the other versions, but "when you're solving, it's important to see". Since "crossed everything out = solved" (dec. 3), a separate "confirm" step is redundant: one version left = the player chose it. Clicking it just crosses back out. We don't add an answer indicator (dec. 9): a non-crossed chip is already bright against the dimmed ones. `yes`/right-click remain only in the tabbed view (UI comparison) |
| 15 | Design tokens instead of ad-hoc values: an elevation ladder of surfaces, 3 text steps, 8 font steps, 4pt spacing, 4 radii ([10-design-spec.md](10-design-spec.md)) | Audit (4 agents): 14 different font-sizes (incl. fractional), 4 shades of gray, text `#566083` with 2.8:1 contrast (WCAG fail), a mishmash of 9/10/12/16/18/20 radii. All "text-background" pairs of the new palette recomputed and pass AA |
| 16 | Flairs - Okabe-Ito palette + an R/B/G/P letter in the chip (not emoji circles) | Circles 🔴🟢 are indistinguishable under deuteranopia (dist 54 against a ~60 threshold), state was conveyed by color alone. A letter works under any CVD and in grayscale; a crossed-out chip keeps its tint (30% mix) and letter - you can see WHAT was crossed out |
| 17 | Orange - only CTA/progress/brand echo; the view switcher is a muted ⚙ panel BELOW the grid; yellow - exclusively hint | Hierarchy audit: 5 competing orange accents diluted the CTA; playtest tools stood above the grid louder than the category headings. Demotion preserves the function (a decision from the methodology: contentious variants - as toggles) while removing the noise |
| 18 | Mobile layer: sticky suspect names + edge-fade on board scroll (do NOT shrink chips below 30px); controls stick to the bottom, the CTA is full-width; result screen: primary - "Send to comments", "Close" - ghost, the final table is collapsed | Mobile audit: names ran off the edge UNREACHABLY (center+overflow), the HUD spilled over by 110px, a white scrollbar; on the result screen the brightest button was "Close", though the core of the community loop is share and voting |

| 19 | Format - **inline post in the feed** (Devvit tall, 512px): header+casebar+controls always visible, the middle scrolls inside (`@media max-height:720px`) | User's decision: "the game right in the feed". The 720 threshold also closes the "gap" at 620-720px where the CTA went below the fold |
| 20 | Result screen - **light card over the dark game**, no scrolling on desktop | User's decision by reference (r/ColorPuzzleGame): "game on dark, results on white"; "short and to the point, everything visible at once". Light tokens overridden locally in the `.result` scope, contrasts recomputed |
| 21 | Distribution histogram - **by solve time** (not by moves) | The number of moves in the cross-out model is almost the same for everyone - the distribution degenerates; time differentiates players. One series: neutral bars #6b7590, accent bar "you" #d9631e (validated: ≥3:1, CVD ΔE 67), labels selective - "you" and the peak |
| 22 | Clue 8 (Paul ≠ Purple) - kept as a crutch for beginners | User's decision; revisit during Case #1 playtests |
| 23 | Game screen = the post itself (~700×512, like feed games on Reddit): brand header, footer and legends removed; HUD (progress/timer, errors - after the first) in the case row; instructions and legend - behind a "?" button; playtest switcher - below the post | User's reference (r/ColorPuzzleGame): "minimum information on the game screen, only the game itself" |
| 24 | Phone - zero scrolling: a compact grid "suspect name above a row of cells" (fits in 390px without horizontal scroll), all 10 clues visible without scrolling | Complaint from a phone: "I don't like scrolling clues and scrolling the board". Trade-off: 24px chips (smaller than recommended tap targets, compensated by an expanded tap zone and long-press names) - verify with a playtest |
| 25 | In the feed (512px post on a phone) the middle scrolls inside the post - it's a preview; full no-scroll is in the expanded view (Devvit expanded) | 10 clues + grid physically don't fit in 390×512; expanding the post on mobile is Devvit's standard path |
| 26 | The "Check case" button and error counter - **removed**. The case closes itself: all 12 cells are deduced and match the solution → ~0.9s pause → slow appearance of the result card. After solving, in place of the hint - a "🏆 Results" button (reopen) | User's decision: "it's clear from the left anyway, when all clues are green". Errors as a concept no longer exist: a wrong full layout simply highlights the violated clues in red - fix it and continue |
| 27 | Solved suspect row animation: a one-time green flash of the cells + a permanent green frame highlight + a ✓-pop by the name | User's request: a micro-celebration for each "closed" character gives a rhythm of progress (4 small rewards before the final one) |
| 28 | Community-building loop **removed**: vote for tomorrow's case theme, submit/suggest a suspect, an in-game deduction card for sharing - deleted | The theme is cosmetic (a reskin: the logical skeleton doesn't depend on it) ⇒ voting for the theme = a facade; the answer is deterministic ⇒ shared deduction cards are the same for everyone = duplicate spam; discussion lives natively in Reddit comments. Return only if participation affects difficulty/logic, not the nouns (the theme) |
| 29 | The "Tabs" view variant and the view switcher (#viewswitch/devbar) **removed** - a single view remains: single board (rows - suspects, columns - categories, candidate chips in cells) | Board beat tabs in playtests. The engine concept "confirmed value" (state 2) was kept intentionally - as an engine hook, not as a UI element |
| 30 | Core promise/tagline updated: the right half "Shape tomorrow's mystery" / "Every solve helps build tomorrow's stranger case" removed. Product = a daily task + an argument about logic in the thread + a streak | After removing the community loop (dec. 28) the promise "you're building tomorrow's case" is unsupported. New: "Daily deduction. One clever case a day" / "Every day, one clever case. Solve it, argue your logic in the thread, keep your streak" |
| 31 | The puzzle's logic and difficulty - **100% deterministic code**, AI is not involved; AI/templates - flavor only (theme, names, items). Engine: [src/server/engine.ts](../src/server/engine.ts) | Whatever the AI produces - it still has to run through the solver (uniqueness + difficulty), and it doesn't reliably hit both targets ⇒ it's cheaper and more deterministic to generate with code. Difficulty is an emergent property of the clue set: you measure it with the solver, you don't write it. See [doc 04](04-puzzle-engine.md) |
| 32 | Difficulty = **TIER by a fork of two solver models** (WEAK: suspect×category board; GRID: all pairwise value×value grids + transitivity). 🟢 weak-forced / 🟡 grid-forced-not-weak / 🔴 not-grid-forced (a guess is needed) | "Whether you need to guess" depends on the techniques available to the player. The old "difficulty = number of clues" doesn't work. Implemented and validated on 10 cases + 20 independent solvers |
| 33 | Default daily difficulty - **🟡**: a fair path without a single guess exists, but it needs cross logic (flair↔time↔item), not reading by rows | User's decision: "I'd always make it yellow, solving was pretty good". The sweet spot: fair (no guessing) + "the brain kicks in". 🟢 - a warm-up, 🔴 - special days for the smart |
| 34 | Reclassification of the manual cases: **Case #0 = 🟡** (the difficulty reference), **Case #1 = 🟢** (too easy - requires not a single cross inference) | Measured by the engine: Case #0 weak-NOT-forced/grid-forced; Case #1 weak-forced. The "clunky" first case is closer to the target bar than the "clean" second one. Naive solvers stumbled on Case #0 precisely because it's 🟡 |
| 35 | "Transitivity depth" as a smooth scale between WEAK and GRID - **rejected** | It doesn't separate the cases (all have 2-3) and doesn't respect "easy = low depth" (Case #1 is also depth 2). The boundary is in fact binary: you run value×value grids or you don't. We keep the fork of two models, without an intermediate one |
| 36 | Hackathon layer in the prototype: full English + de-Reddit ("Coat" instead of flair, Omar instead of ModBot, no r/-mentions in visible strings; "flair" in the UI replaced with "rank"); confirm() and placeholder buttons removed | TIER 0 strategies (removing the "Russian" and "on-the-nose theming" penalties); run through an adversarial content audit: Cyrillic 0, Reddit terms 0, placeholders 0 |
| 37 | Honest numbers: when N<50 solvers - "You're the Nth detective today" instead of a percentile and histogram; the distribution and % - only when N≥50 from the same buckets (one number in the hero and on the chart). The "day 2 / at scale" playtest toggle below the post shows both states | De-slop from the strategy: fake social proof = minus trust in everything. The toggle is a demo tool, labeled as playtest |
| 38 | Onboarding = 3 coach marks over the real case (clues → chip → progress), skip in 1 tap, the timer is paused until the tutorial ends; ?tut=1/0 hooks | TIER 0 p.6: "learn it in 15 seconds", nobody reads the text behind "?" |
| 39 | **[REMOVED 2026-07 - stripped out, see dec. 41]** "Detective trail": suspects in the grid numbered ①-④, after solving - a copyable string "Case #N · time · path ②→①→④→③ · 💡n · clues #… untouched". Order = the order rows were closed; "untouched" = clues that got satisfied only on the last move; undo rolls back this bookkeeping too | TIER 1.1: the share artifact encodes the PATH, not the answer (analogous to the Wordle grid); numbering in the grid makes the string readable without a legend |
| 40 | **[2026-07: live, NOT removed - the 🔥/⚖️/🌿 vote + "envelope" = the mechanism of a fair 🟢↔🟡 ramp; see dec. 41]** Community verdict: solvers vote 🔥/⚖️/🌿 (one vote per case, "play again" doesn't reset it) + a "sealed envelope" CASE #N+1 with a "HARD leads - 62%" ticker and a countdown; an epilogue line "🥇 Yesterday: u/X · 2:41 · N detectives · voted TIER (pct%)" in the post header (on a phone - a short form); rank in the result stats | TIER 1.2-1.3: visible causality of the choice + recognition by name + a public ledger. Launch texts - docs/12-launch-content.md |
| 41 | **(2026-07) Rebranding Logic Thread → Deducto** + cleanup of the share layer. App slug `deducto-puzzle` (`devvit.json` `name`); the "Deducto" brand set in the post title, menu items ("Deducto: publish a case" / "publish showcase"), `src/shared`, `src/server`, `src/client`. **The "detective trail" (dec. 39) stripped out**: the copyable share string in the client no longer renders (in `main.ts` only a dead reference to it remains in a comment) | The rename went through all the code in one sweep. Trail dropped per dec. 39: the string encoded the path but gave little value and cluttered the result card. IMPORTANT for the reader: the community verdict (dec. 40) is NOT removed by this - the 🔥/⚖️/🌿 vote and the "sealed envelope" are alive (`#r-tomorrow`, `renderVote`, `/api/vote`, seal `SEALED`) and remain the mechanism of a fair 🟢↔🟡 ramp (dec. 32-33), unlike the cosmetic theme loop removed in dec. 28. It's hidden only on the showcase/judge post (which is decoupled from the ramp). The solution + timer are still server-side (anti-cheat), the client doesn't see the solution |
| 42 | **(2026-08-07) Default daily difficulty - 🟢. Decision #33 (default 🟡) is REVERSED.** `DEFAULT_LEVEL = 0` in [src/server/index.ts](../src/server/index.ts); 🟡 now arrives only when the sub votes Harder (`levelFromVote`, already built). Rollback is one constant | **Measured, not argued** ([scripts/check-solvability.ts](../scripts/check-solvability.ts), reproducible): run the WEAK model - literally everything the 4×3 board can record - over the bank and count how many of the 12 cells it forces. 🟢 **12.00/12** on every case. 🟡 **0.73/12** mean; **69 of 100** yellow cases give **0/12** and 98 of 100 give under 6/12. So the default handed a first-time player a board on which no move follows from what they had written down - and no error either, just nothing happening. That is the "too hard / didn't get it" drop-off, and it is structural, not a tuning knob: `classify()` DEFINES 🟡 as `grid_forced && !weak_forced`, i.e. "the value×value grids solve it, the board does not". Rejected alternatives: keep 🟡 and add a value×value layer to the board (reverses decisions #1/#29, +1-1.5 weeks of client work); ship 🟢+🟡 in one post (doubles the UI, breaks leaderboard and streak). See `plans/02-difficulty.md` (internal) §A0/C1 |
| 43 | **(2026-08-07) New tier `tutorial` - an over-clued 🟢, and it never appears as a daily.** `_minimize` stopped at a floor of 14 clues instead of stripping to the bone, plus two shape filters: relational clues (`before`/`nsame`) ≤ 35%, and ≥2 `same` clues anchoring a value to a TIME. Bank: 15 tutorial / 45 green / 100 yellow. `LEVEL_BUCKETS` is built from `DAILY_TIERS = [green, yellow, red]` - `tutorial` is deliberately not in it, so no vote can land the daily on it; it feeds only the warm-up lane | "🟢" was never the same thing as "easy": green spans score −2…54. What makes a case hard is the **minimization** - it strips every clue that isn't load-bearing, so each step has exactly one route in. Stopping at 14 leaves the case over-determined. Clue count alone isn't enough, hence the shape filters: measured, a plain random strip to 14 keeps mostly relational clues and only **1 case in 60** passes them; offering relational clues for removal first takes that to **59 in 60** while keeping the shapes varied. Biasing on more than that one band collapses every case to the same 8-hook skeleton - a transcription exercise, not a puzzle |
| 44 | **(2026-08-07) Guest result screen: no numbers at all.** `guestResults()` deleted; `/api/check` answers a guest `{status:"solved", guest:true, results:null}`. The client shows "sign in to record your time and streak" + `showLoginPrompt()` | It was returning `streak: 1, rank: 1, betterPct: 50` - all three invented. That is exactly the fake social proof decision #37 exists to forbid, and it is the kind of thing a Reddit reviewer notices |

| 45 | **Decision 25 REVOKED (2026-08-07): no scrolling anywhere, at any size.** `html, body { overflow: hidden }` in [base.css](../src/client/base.css) makes a scrollbar structurally impossible; the five scroll containers (`.main`, `.clue-list`, `.grid-wrap`, `.overlay`, `.notice`) are gone | Dec. 25 legitimised "the middle scrolls inside the post - it's a preview". The Reddit [Featuring Guide](https://developers.reddit.com/docs/guides/launch/feature-guide) states plainly: **"Scrolling within inline webviews is prohibited."** A rule of the platform beats a local trade-off, and without it the game is not eligible for the Games Feed or the Community Drawer |
| 46 | **Two entrypoints (2026-08-07), superseding dec. 19 and 23.** `default` → `splash.html` at height `regular` is what the feed shows; `game` → `index.html` opens fullscreen via `requestExpandedMode(event, "game")`. `devvit.json` `post.entrypoints`, `src/client/vite.config.ts` | The same guide requires "a custom first screen"; a post that loads straight into the board has none. One change buys both requirements: the feed gets a self-explanatory first screen that always fits, and the board stops being squeezed into 512px. Expanding reloads the document, so state must live server-side - `/api/daily` already returns the saved grid, hint count and elapsed time |
| 47 | **Decision 24 revised (2026-08-07): chips 49-54px wide, 24-48px tall** (was a flat 24×24 with an expanded tap zone). One board renderer; the chips wrap 1×4 or 2×2 by themselves and the four suspect rows split whatever height the clue list leaves | Measuring beat the assumption. The 24px chip was blamed on the 512px post height, but the binding constraint is **width**: three categories × four values = twelve chips across the screen, so at 375px no chip can exceed ~28px wide while all three sit side by side. Wrapping each cell to 2×2 doubles the width and pays for it in height, which fullscreen actually has. Result: WCAG 2.2 AA (24×24) is met on every tested size, and 44×44 from 900px up - not the "≥44 everywhere" the plan hoped for, and the arithmetic says why |
| 48 | **Decision 20 REVOKED (2026-08-07): the result is a screen in the same palette, not a light card over the dark board.** `body[data-view]` swaps `#view-game` / `#view-result`; the local token override inside `.result` is deleted, sections are laid out with air instead of `<details>` | Two token systems in one app made the result read as a second application (plan 03, section B), and the light card only existed because it was a modal over a dark board. Once it is a screen of its own the dissonance has no reason to exist - and deleting the override is what makes a palette swap a change to [tokens.css](../src/client/tokens.css) rather than a rewrite |
| 49 | **Decision 18 partially revoked (2026-08-07): horizontal board scroll, sticky suspect names and the edge-fade masks are removed** (`updateGridFade`, `updateClueFade`, `.fade-r`, `.fade-b`) | They existed to make a scrolling board bearable. There is no scrolling board any more, so they are dead weight - and an edge-fade over a board that fits is a lie about there being more content |
| 50 | **Synthetic showcase data removed (2026-08-07):** `DEMO_COUNTS` / `DEMO_LB` deleted; the result screen shows a histogram only when the server reports `hasHistogram` (N ≥ 50), and the leaderboard only real entries | Consistent with dec. 37 ("honest numbers"), which the demo constants quietly contradicted. A sample histogram and three invented usernames are fake social proof on a public post - a real risk at Reddit review, and the labels ("sample", "demo") were never going to be the thing a reviewer remembers |

| 51 | **(2026-08-07) The difficulty mark is a four-rung ladder living in one shared module,** [src/client/tiers.ts](../src/client/tiers.ts): ⚪ warm-up, 🟢 green, 🟡 yellow, 🔴 red, each with a label and a tooltip. Both entrypoints import it; an unknown tier gets **no** mark and its raw name | The badge mapped "anything that is not green and not red" to 🟡, so the tier `tutorial` added in dec. 43 - the gentlest case in the bank - was shown to a first-time player as the hard one. Two entrypoints held two separate maps, which is exactly how that drift happens twice. ⚪ reads as "not even green yet" on a traffic-light ladder, and falling back to a *guessed* mark is worse than falling back to none: a badge that quietly calls an unseen tier "green" lies about the case |
| 52 | **(2026-08-07) First-run ladder: the warm-up is offered before today's case, and leaving it re-fetches instead of reloading.** A card over the board offers "Start the warm-up" / "Skip to today's case"; the offer is `meta.showWarmup` from the server and self-closes (one solved warm-up, or `POST /api/practice/skip`). Coach marks then run on whichever board the player chose. `location.reload()` in the practice exit is replaced by a fresh `GET /api/daily` | The warm-up existed but was unreachable: an unlabelled ▷ between ? and 💡, never offered, and never rotating. Skipping had to be recorded server-side or the card would return on every reload. The reload had two failure modes of its own - it threw away the "warm-up done, today's case is harder" hand-off, and for a **guest**, whose finished warm-up the server cannot record, it put the offer straight back on screen: an infinite loop |
| 53 | **(2026-08-07) The hint ladder is a panel with the price on every button** (rung 1 "Which clue works" FREE, rung 2 "What it rules out" FREE, rung 3 "Reveal a cell" COSTS 1), and pressing 💡 takes the free rung 1 immediately. The client phrases all four `Advice` kinds - move / contradiction / stuck / done | Two thirds of the ladder is free and teaches instead of answering, which is worthless if nobody presses it - so the price is written on the button, not buried in help text. Wording rule for **stuck**, from the measurement in the row below: never "there are no moves on this tier". A 🟡 board is not empty at the start; it runs out at the WEAK fixpoint minutes in. The copy says "no single clue moves this board **any further**" and hands over the actual technique (link two categories through a shared value) |
| 54 | **(2026-08-07) The stall nudge is decided server-side and merely displayed by the client.** `/api/state` returns `nudge:true` once, after >180 s of active play with >90 s and no new cell; it rides the 10-second heartbeat, not the move autosave | Every input already lives on the server - active time, when the board last gained a cell, whether the offer was made - and duplicating that clock in the client would be a second source of truth to keep in sync. It has to ride the heartbeat because the move autosave is silent by definition when a player has stopped moving |
| 55 | **(2026-08-07) Between the two "is anything forced right now" models, the server's wins.** `boardStuck()` prefers `adviseOnGrid`'s verdict whenever it holds one for the board on screen, keyed by a signature of the grid so a single move retires it; [forced.ts](../src/client/forced.ts) is the offline fallback. Closes the TODO left in that file | The client model is an approximation that runs on every render at no cost; the engine's is exact but needs a round trip. Ranking them gets both properties instead of choosing. Measured on a fresh board across all 160 cases they agree 160/160 and **neither** calls a single one stuck - which is the evidence behind the wording rule in dec. 53 |
| 56 | **(2026-08-07) The guest solve gets its own screen, not a hollowed-out result card** (`body[data-view="guest"]`): "Case closed", the time from your own clock explicitly marked *nothing was recorded*, what logging in would have counted, and `showLoginPrompt()`. Completes the client half of dec. 44 | The result card is built out of rank, percentile, histogram, streak and the difficulty vote - for a guest every one of those is empty, and an empty version of it reads as a bug. Naming the one thing that is missing, and the one action that fixes it, is both honest (dec. 37) and the only conversion moment the game has |

### D3 "Manila" applied to the client (2026-08-07)

Rows 57-70 record the application of [10-design-spec.md](10-design-spec.md) to `src/client`. Rows
57-62 are the reversals the spec's §6.2 mandates; rows 63-70 are the calls the spec did not cover,
because the screens they concern (guest, hint ladder, nudge, warm-up lane, standings post) were
written after the prototype and do not exist in it. The one idea everything below follows from:
**colour belongs to evidence, chrome is ink on paper.**

| # | Decision | Reason |
|---|---|---|
| 57 | **Decision 17 REVERSED. There is no orange and no yellow anywhere in the client.** The CTA is a filled `--ink` button; the hint slip is `--sheet-raised` with a 3px `--ink-3` left rule. The whole legacy hint colour group (`--hint-bg/border/fill/fill-hover/line`) collapses to zero tokens, as do `--ok-*`, `--info-*`, `--bar-*`, `--vote-*`, `--surface-3`, `--border-active/hover`, `--casebar-top` and every `--shadow-*` | Dec. 17 demoted five competing oranges to one; D3 removes the competition instead of ranking it. With four saturated coats in a 4×3 evidence grid, any hue in the chrome reads as a fifth piece of evidence. The gradient casebar, the drop shadows and the second and third grey each existed to separate panels that a hairline separates better - `--rule-strong` clears 3:1 on all four surfaces in both themes, which no legacy border token did (`--border-strong` was ~2:1 on its own surface) |
| 58 | **Decision 18 REVERSED (the half that survived dec. 39/41): the result screen has no filled button at all.** Footer is `Full solution` (ghost) + `Play this case again` (secondary). The game screen's filled button appears only at 12/12, on `See results` | "One filled button per screen, maximum" is only a signal if it is spent on something. After a solve the primary act is to leave; the thing worth touching is the difficulty vote, which is three secondaries. Keeping a filled button there would have made "filled" mean "the biggest thing on the screen" instead of "the case is closable", which is what it means during play |
| 59 | **Decision 20's cleanup finished: there is not one local token override left in `src/client`.** Verified mechanically - `grep -rn "#[0-9a-fA-F]\{3,6\}" src/client/*.css src/client/*.ts \| grep -v tokens.css` returns nothing, and no rule redefines a `--token` outside [tokens.css](../src/client/tokens.css) | Dec. 48 deleted the `.result` scope override; D3 removes the reason anyone would add another. The result is now a raised sheet one step up the same elevation ladder (`--sheet-raised` on `--paper`), so "results look different" needs no second palette. That is what makes the next visual language a rewrite of one file rather than of every component - the contract [tokens.css](../src/client/tokens.css) states at the top of itself |
| 60 | **Decision 21 amended: the histogram stays by solve time, and its series becomes achromatic** - hollow `--groove` bars with a `--rule-strong` boundary, your bin filled `--ink`, a `YOU` caps label under the axis. The per-bar count moves from a printed label into the bar's `title` | The old pair `#6b7590` / `#d9631e` was a hue carrying "you", i.e. colour outside evidence. Filled-vs-hollow plus a printed axis label is two non-colour channels for the same fact, and it is what the dataviz method asks for when one series has one highlighted member. The counts came off because at eight bins on a 360px phone they were noise stacked on top of the shape that already shows them |
| 61 | **Decision 23 amended: the board carries a standing one-line instruction** - `TAP A CANDIDATE TO RULE IT OUT · THE LAST ONE STANDING IS THE ANSWER`, mono caps in `--ink-3` under the grid | Dec. 23's "minimum information on the game screen" holds for chrome, not for the rules of the game. Reddit's Featuring Guide makes self-explanatory design a gate ("anyone should be able to click into your post and have the context needed to learn, play, or participate"), and a board of coloured squares with no verb on screen does not clear it. One 11px line is the cheapest way to pay for it |
| 62 | **Decision 16 re-verified, not revoked.** Okabe-Ito coats + the R/B/G/P letter carry over unchanged. Two asymmetries are now explicit: blue is theme-dependent (`#0072B2` light / `#56B4E9` dark) and the letter ink is a per-coat token, not a global one | Forced by the numbers, both. A dark letter on `#0072B2` is 3.37:1 (fail) so light blue needs a paper letter, while `#0072B2` as a fill vanishes on a dark sheet; and forcing one letter colour on all four coats breaks at least one of them. Measured again on the new ground: coat fills separate by ΔE00 ≥ 10.4 under every CVD type, and alive-vs-crossed - the channel that actually matters during play - holds ΔE 21.8-45.1. The crossed washes still collapse under deutan/tritan (ΔE 1.9-3.9); that is accepted and load-bearing on the letter, exactly as before |
| 63 | **(2026-08-07) The tier mark stops being an emoji traffic light and becomes the text of a `.stamp`: WARM-UP · TIER I · TIER II · TIER III.** [tiers.ts](../src/client/tiers.ts) keeps its four rungs, its labels and its "unknown tier gets no mark" rule from dec. 51; only `mark` changes. In the warm-up lane the stamp is empty, because `WARM-UP 3/15` in the case slot already says both things | D3 takes emoji out of the chrome, and the traffic light was the largest remaining one. It also had two failure modes the stamp does not: it died in greyscale (🟢 and 🔴 are near-identical in luminance) and it depended on the webview's emoji coverage - `⏱` without VS16 already rendered as tofu in the legacy Firefox build. A ladder that reads in order (WARM-UP < I < II < III) survives CVD, greyscale and a bare font stack |
| 64 | **(2026-08-07) The status line and the board instruction share one slot, and it is achromatic.** `#statusline` shows the instruction when the board has nothing to say, the contradiction count when a clue is contradicted, and the "no forced move left" teaching line at the WEAK fixpoint. No `k-err` / `k-info` surfaces | Two stacked lines cost ~30px of phone height for content that is never both at once. Achromatic because of the rule of one red: on the game screen red already means *this clue is contradicted*, and it is spent on the clue row itself (left rule, number box, flag line). A second red mark saying the same thing in different words would be the system's one rule broken for emphasis it does not need |
| 65 | **(2026-08-07) The CLUES segmented meter is replaced by a tally in the clue-panel head** (`1 CONTRADICTED · 3 SATISFIED · 6 OPEN`, with the contradiction count in `--stamp`); DEDUCED becomes 12 countable `--rule-strong` squares that fill with `--ink` | The clue meter was the one place hue did real work in the old chrome - green segments for satisfied, red for violated - and it is exactly the work D3 forbids. Words do it better anyway: a bar of ten segments answers "how many" only after you count it, while the tally is already a number, and it sits on the panel it describes instead of two components away. The cell meter keeps its bar because 12 squares are countable at a glance and carry no hue |
| 66 | **(2026-08-07) The hint slip docks in flow under the clue column instead of floating over the board, and `body.slip-open` pays for the height below 900px** by dropping yesterday's epilogue and the standing instruction | A hint that says "clue 4 rules out 09:00 for Mira" is useless while covering the board it is talking about - that was the old floating `.notice`'s failure. In flow it costs ~120px, which on a stacked phone layout has to come from somewhere, and measurement said it was taking it from the clue list: clue 10 silently disappeared at 393×852. The two lines that yield are the least urgent thing on screen and the one the hint has just made redundant. **The clue list never yields a row** - every clue staying readable is the whole point of a no-scroll layout (dec. 24/45) |
| 67 | **(2026-08-07) The guest screen's one red mark is a `NOT RECORDED` stamp** where the result screen carries `CLOSED` | The rule of one red says red is the mark made *on* the file, one meaning per screen. On the guest screen the single true statement is that nothing was written down - so that is the mark. It also puts the honest part (dec. 44: no invented streak, rank or percentile) at the top of the screen in the one element that carries weight, rather than only in the paragraph below |
| 68 | **(2026-08-07) The full solution moves off the result sheet onto a second face of it,** reached by the `Full solution` ghost button in the footer. Pressing it swaps histogram + standings + vote for the table, and back. This overrides the "laid out plainly rather than folded into `<details>`" half of dec. 48 | Measured, not argued: with the table inline the sheet was 884px of content, which fits nothing between 700px and 900px of viewport height - it was cut off at 393×852 and 360×800 (the two commonest phones), and `overflow:hidden` means cut off *silently*. The spec's own result order already puts `Full solution` in the footer beside `Play this case again`, so the fix was to apply the spec rather than to invent a compromise. Dec. 48's objection was to `<details>` on a screen that had room; this is a screen that does not |
| 69 | **(2026-08-07) Decision 27 REVERSED: no confetti, no green cell flash, no green frame, no ✓-pop.** A solved suspect row is a hollow 8px box next to the name turning into a filled `--ink` one | Every one of those was hue in the chrome, and the confetti was hue built out of the *coat* colours - the evidence palette spilled onto the result screen as decoration. The box also fixes what the green frame never did: it reads in greyscale, under all three CVD types, and it is in the row head where you are already looking, not around cells you have finished with. The micro-celebration survives as a 300ms scale pop on the box |
| 70 | **(2026-08-07) The ①-④ suspect numbering is removed from the board and the hint log; leaderboard medals 🥇🥈🥉 become plain ranks.** Podium is position plus weight; `You` is a filled ink tag, a qualifier like `FLAGGED` is the same tag outlined | The numbering existed for the "detective trail" share string, and dec. 39/41 stripped that out - it has been a marker pointing at nothing since. Keeping it would have put two marks (① and the hollow □) on one row head. The medals are emoji in the chrome by the same rule as dec. 63, and they were doing a job the rank number already does |

### Density, pointer feedback and standings for everyone (2026-08-08)

Rows 71-75 come out of the owner playing 0.0.7 on a real device: *"the visuals don't quite land.
Everything merges. Hard to tell the blocks apart"* (desktop; mobile was fine), *"the ✕ on the hint
notices doesn't look great"*, *"not every button lights up when I hover"*, *"lots of empty space in
the preview"*, and *"maybe the leaderboard shouldn't need a special call from the menu - let any
player look at it… I pictured a table inside the game that anyone can study."*

| # | Decision | Reason |
|---|---|---|
| 71 | **The surface ladder is re-cut, and `--shadow-*` comes back as exactly one token - reversing that clause of dec. 57.** Light: `--paper` `#E7DFD1 → #BEAD8C` (a real manila tan, **2.00:1** under `--sheet`), `--sheet` `#F8F4EB`, `--sheet-raised` `#FFFDF8`, `--groove` `#D9CFBA`, `--rule` `#D2C8B5 → #BEB299` (1.48 → **1.91:1** on the sheet), `--rule-strong` `#7E725C → #655B47` (still ≥3:1 on all four surfaces, now including the darkened paper). Dark is re-cut on the same criteria, plus `--ink-2/-3` and `--stamp` lightened to keep AA on the raised sheet. `--shadow-1` is two hard offsets and one 8px blur, both `color-mix`ed out of `--ink` - a printed drop, not a photographic one - and is used only where a sheet actually floats over another surface | Measured, and the measurement is the point: the four surfaces shipped inside **1.09-1.46:1** of each other, and a lightness step only becomes an edge around 2:1. The result card sat on the page at 1.29:1, i.e. it had no boundary at all. The metaphor already said what to do and the first cut ignored it - `--paper` is the folder on the desk, `--sheet` is the sheet laid ON it, and they were never meant to be the same tone. Two ceilings are now stated rather than discovered: a surface carrying `--ink-3` at AA cannot go below lum .583, which caps `--sheet`/`--groove` at 1.51:1; and `--sheet-raised` is capped by white, so `--sheet`/`--sheet-raised` can never pass ~1.1:1 - that pair is carried by a rule, a left rule and the shadow, and the audit says so out loud instead of leaving it out. Consequence, stated as a rule in tokens.css and **asserted on every screenshot** rather than trusted: nothing but `--ink` may be set on `--paper` (`--ink-2` is 4.43:1 there, `--ink-3` 3.40:1) |
| 72 | **Structure is drawn in `--rule-strong`; `--rule` divides rows and nothing else.** The board grid, the clue-panel head, the sidebar edge, the facts strip and the `.b-meta` strip move from `--rule` to `--rule-strong`; the gap between result sections goes `--sp-2 → --sp-4` | The spec's §1.3 has said "structure + every control boundary: chips, secondary buttons, table head, **board grid**" since it was written, and the application pass drew the board in `--rule` anyway - a 1.48:1 hairline, which is a grid you have to take on faith. That single line is the largest reason a 4×3 evidence grid read as a field of loose chips. And half of "everything merges" was never contrast at all: sections were touching, and a hairline between two blocks that already touch is not a boundary anyone reads |
| 73 | **Every interactive element answers to the pointer, and every hover rule in the client now lives inside `@media (hover: hover)` guarded against `:disabled`.** Board chips become real `<button>`s with `aria-pressed` (pointer cursor, focus ring, Enter/Space, and a hover that takes the border to `--ink` - the one channel that reads on a saturated coat as well as on paper); the selected tab reacts too; the hint slip's `✕` moves out of the corner onto a `--groove` head rail, sharing a baseline with the slip's label and its pager | The chips were `<span>`s with click listeners: no cursor, no hover, no keyboard, and an `aria-pressed` that the spec's own CVD audit claimed as a non-colour duplicate and that did not exist. The hovers that did exist were unguarded, so on a touch screen they latched after a tap and left a button looking pressed - including the destructive red the spec describes as "absent on touch". The `✕` was a 28px box positioned 2px off the corner, aligned to nothing; on a rail it is furniture. The rail costs ~25px, which dec. 66 forbids taking from the clue list, so at 667px of height the clue rows tighten by 2px each instead |
| 74 | **The splash card fills the feed slot** - one column, identity at the top, the CTA and the social line on the bottom edge, `--fs-2xl` serif title, `max-width` 560 → 720 | It was a 560px box centred in a 320px-tall webview, so roughly half of the slot was empty page around it. That slot is the first and often only thing anyone sees of Deducto, and Reddit's Featuring Guide asks for a *compelling* first screen; a card floating in the middle of an empty rectangle is not one |
| 75 | **The standings are open to every player, from inside the game.** A screen of its own (`body[data-view="standings"]`) reached from the board's control bar - so a player who has not solved anything can study the table - plus the four tabs on the result sheet made real: TODAY (by time, with hint counts), WEEK, **STREAK** and ALL-TIME, each the full list scrolling inside its own box with your row pinned under it. One implementation - [ledger.ts](../src/client/ledger.ts) + [ledger.css](../src/client/ledger.css) - serves all three surfaces including the standalone weekly post. **This reverses [02-gameplay.md](02-gameplay.md)'s "the full week / all-time / longest-streak boards do not live on the result screen at all"**; the "while solving, do NOT show a leaderboard" half stands - the standings never appear over a live board, only on an explicit press, and close straight back to it | `GET /api/leaderboard` has been public since plan 04 (`/api/*`, not `/internal/*`), and the only way to reach a board was solving the case or being a moderator with a menu item. The STREAK board existed on the server with nowhere to appear. Entry point is the icon rail rather than a fourth labelled button because at 360px "Warm-up · Hint · Standings" makes the control bar 380px wide - measured, not guessed - and the label comes back at 720px where the bar can carry it. Four rows on the result sheet is what the sheet can spare once the hero, the facts, the histogram and the vote have taken theirs; "See all" opens the same screen at full height |

### Readability: the second pass on the same complaint (2026-08-11)

Rows 76-84 come out of the owner playing 0.0.8 on a real device, after dec. 71-75 had already
raised the surface contrast. He still said *"the blocks don't separate well. Lots of text, lots of
small details"* - so contrast was not the whole cause - plus *"hard to see in the design when you
select a square"*, *"the pre-screen feels very empty"*, *"TIER II - could be marked with colour
too, if I understood right that it's about case difficulty"*, *"the note doesn't appear on top, it
shifts the clues"*, *"is that a hint? …that too? I don't understand the point of this text"*,
*"after the warm-up I'd like a choice of buttons, not an automatic jump"*, and *"you can't see how
all-time points are counted; and what is **still running**?"*.

The thread running through them: **every one is a channel that was technically present and
practically unreadable.** A 2px ring on a 27px chip, a rung with no scale, four messages in one
voice, a formula ellipsed at the word "MINUS". None of them are fixed by more contrast.

| # | Decision | Reason |
|---|---|---|
| 76 | **"Deduced" stops being an outline and becomes a 4px ink frame INSIDE the chip** - `border-color: --ink` + `box-shadow: inset 0 0 0 2px --ink, 0 0 0 1px --ink`, replacing `outline: 2px --ink; outline-offset: 2px`. The row-closed box goes 8px/1px → 10px/1.5px and the closed row gains two free channels: the name at 700 and an inset `--ink` bar on the name strip | Arithmetic, and it was available before the fact: a phone chip is 26-28px with a **3px** gap to its neighbour, so a ring drawn 2px outside it lands in that gap and merges with the neighbouring chip's own 1px border. On a board where every chip, every cell and every row is already bounded by a line, two more merged hairlines are not a channel - and it failed worst on the mid-luminance coats (🟩 G, 🟪 P), exactly as the owner reported, because a thin dark line there has no mass behind it. There is no room outside the chip, so the mark goes inside, where the chip owns every pixel: four times the mass of any other line on the board, pure `--ink` on every coat and on paper alike, for 3px of glyph box. It also stops colliding with `:focus-visible`, which owns `outline` and now has it to itself. **Contrast, stated rather than assumed:** the frame clears 3:1 against every coat fill in light (3.41-5.77:1, blue lowest) and against three of four in dark it does not (B 1.96, P 2.60, G 2.91, R 3.29) - but its outer 1px sits on `--sheet` at **13.08:1 dark / 16.1:1 light**, which is the adjacent background SC 1.4.11 actually asks about, and is the same argument the spec's §2.2 footnote † already makes for the chip border. The old outline sat *entirely* on `--sheet` at that same 16:1 and was still unreadable, which is the whole lesson here: a passing ratio on a 2px line that merges with its neighbour is a number, not a channel |
| 77 | **The tier stamp carries the rung AND what the case asks of you**, on two lines - `TIER II` over `LINK TWO CLUES` - and the ladder's second channel is the stamp's **weight**: `--ink-3` outlined, `--ink-2` at 2px, `--ink` at 4px double. [tiers.ts](../src/client/tiers.ts) gains `gloss` and `rank`; `paintTier()` replaces `tierBadge()` at both call sites | The owner asked for colour and the request under it was "this mark doesn't tell me anything" - *"if I understood right that it's about case difficulty"*. Colour is the one channel D3 will not spend on chrome (it belongs to evidence, §0) and it is also the weakest one available: it dies in greyscale and under CVD, which is why dec. 63 took the traffic light out in the first place. Fixing the intent instead of the letter means the mark has to say the thing in **words**: the labels already existed in `tiers.ts` ("Solvable on the board" / "Needs cross-referencing") and were reachable only through a `title` attribute nobody hovers on a phone. Glosses are capped at 14 characters because the stamp shares the casebar with the case title and the splash header with a 26px serif one - measured: an 18-character gloss took the splash title from one line to two |
| 78 | **Decision 66 REVERSED: the hint slip is an overlay again** - absolutely positioned inside `.grid-area`, `data-anchor` flipping it to the far end of the board from the suspect the hint names. Every `body.slip-open` reflow rule is deleted; nothing on the screen moves when a hint opens | The owner asked for it and he is right, but the measurement is what settles it: dec. 66 put the slip in flow to stop it covering the board, and in flow it comes out of the **clue list's** height budget. On the over-clued 14-clue case that is `#clue-list` overflowing its own box by **+80px at 375×667, +31px at 360×800, +5px at 393×852** - and `overflow: hidden` means the last clues are cut off in silence, which is precisely the failure dec. 66 was written to prevent. So both placements had the same bug and only one of them also moved the clues. The anchor flip is what keeps dec. 66's actual requirement: with four rows and a slip at most half the board's height, the end opposite the named row is always clear, so the slip can never cover the cell it is about |
| 79 | **Four things that spoke in one voice are separated by form, place and moment.** The standing instruction is a sentence-case sans caption under the board; the contradiction count stays in that slot at full ink because it is the board describing itself; the **dead-end verdict moves out of it entirely** and only ever answers a press of Hint; the stall nudge becomes a bar on the bottom edge with one line and one action. `notice()` is deleted - the slip is the hint ladder's and nothing else's - and every acknowledgement ("Board cleared", the help text, the warm-up blurb) is a floating toast. Every advice string is cut to one sentence | The owner quoted **four different things** back and could not tell them apart: *"is that a hint? …that too? I don't understand the point of this text."* They were the board instruction, the "no forced move left" verdict, the stall nudge and the ladder's own rung labels - and all four were 11px mono caps or 13px sans inside the same box, in the same place, at the same weight. An instruction that is always true is a caption, not a message; a verdict about a technique is an answer to a question, so it waits to be asked; the nudge is the only thing that arrives on its own, so it is the only thing that looks like an interruption. [forced.ts](../src/client/forced.ts) keeps its job under dec. 55 - it now decides what the nudge **offers** (a pointer, or the technique) rather than what the instruction says |
| 80 | **The result sheet goes from nine groups to four.** The histogram is drawn only from 860px, where the two-column layout has room for it; the dashed "sealed envelope" and its `SEALED` stamp are gone and the countdown rides the section heading; the vote bar appears only after you vote; `Standings` as a heading over a `TODAY` tab is dropped; `TOP 20 OF 142` moves onto the button as `See all 142`; `AHEAD OF 74%` comes off the pinned row. Spacing rhythm goes from three near-identical steps (12/16/4) to two (4 within a group, 20-24 between) | Counted at 393×852, before and after: **14 visual groups → 10**, and **64 runs of text on the frame → 43** (a "run" is a visible element that directly owns a non-empty text node, counted by the screenshot harness with scrolled-out rows excluded; the before figure is the after figure plus the 21 runs the removed blocks carried - 10 for the histogram and its axis, 1 heading, 6 for the vote legend, 3 for the envelope, 1 for `AHEAD OF`). Contrast had already been raised and the sheet still read as a wall, because separation is not only contrast - if every gap on a page is the same size, no grouping exists to read. Each removal is a duplicate, not a feature: the histogram is a picture of the hero's own sentence ("faster than 68% of 142 detectives") and was already hidden below 700px of height for that reason; the vote bar before you vote answers the question the three buttons are asking; `AHEAD OF 74%` is the hero line again, 300px lower, and on a 375px phone it was squeezing the name down to `u/…` |
| 81 | **The pinned "you" row is drawn only when your row is not visible in the list.** Measured on the rects, not guessed from an index, because the two surfaces differ (four rows on the result sheet, a full screen in the standings) and both scroll. `board.ts` (deleted by dec. 85, below) drops it and refills the list, since hiding it frees a row's worth of height | It exists so a player at rank 340 can still see their own line. When that line is already on screen it is the same name printed twice in a row - and on a board where you are the **only** entry, which is what the owner hit, the screen was your username, then your username again |
| 82 | **Solving a warm-up ends in a choice, not a timer.** A card with a `WARM-UP SOLVED` stamp, a headline and two buttons: `Open today's case` / `One more warm-up`. The 1.3s `setTimeout` into `exitPractice()` is gone | *"After the warm-up I'd like the note highlighted, and not an automatic jump to the case but a small choice of action buttons."* The lane holds 15 rotating cases (dec. 43) and had exactly one exit, taken for you before you had read the message explaining it. A grey plaque is also the wrong dress for the one moment in the lane worth marking |
| 83 | **The standings say how a figure is arrived at, in a line that fits.** `boardMeta` states the arithmetic ("500 points a case, more for speed, 60 off per hint (300-1000)") and `.st-meta` wraps to two lines in sentence-case sans instead of ellipsing one line of mono caps. `provisional` stops being rendered as **"still running"** and becomes **"scores settle overnight"** | Both are the same failure: a string that names a flag instead of stating a fact. `POINTS = SPEED MINUS…` ended mid-formula at 360px - the explanation went missing exactly where it was needed - and "still running" is the internal name of the `provisional` field, which tells a player nothing about what it means for them (their score can still move tonight) |
| 84 | **The splash grows a docket** - the file's own figures, in the middle of the slot dec. 74 had stretched but left empty. Your board as 12 countable squares (the game's own `.ticks`), your streak, solved today, fastest, and the next case's countdown once today's is behind you. From 560px it is a ruled column on the right of a two-column card; below that a strip of three cells | Everything in it was already in `GET /api/preview` and was being spent on one 11px line of caps at the bottom edge. The empty middle was most of the slot at 640×320, the commonest desktop feed width, and Reddit's Featuring Guide asks the first screen to be *compelling*. Twelve hollow squares are also the cheapest possible answer to "what is this game": it shows the mechanic before anyone has pressed anything. Still no invented numbers (dec. 37) - a case nobody has closed says so in words rather than showing a zero |
| 85 | **The standalone weekly standings post is removed** - the `leaderboard` entrypoint, `board.html`/`board.ts`/`board.css`, `GET /api/board`, `latestPostUrl()` and the `Deducto: publish weekly standings` menu item are all deleted; `routes.test.ts` pins their absence with 404 assertions. Decisions 75 and 81 stand, but their "three surfaces" is now two: the in-game standings screen and the block on the result sheet. `ledger.ts`/`ledger.css` are unchanged - they were already one implementation, they now have one caller fewer | Owner's call. Once every player can open the same four boards inside the game at any moment (dec. 75), a separate weekly post is a third copy of numbers they already have, paid for with a manual moderator ritual every Monday. The counter-argument was real and is recorded here so it can be revisited: with automatic daily posting deliberately off, a subreddit has very few non-spammy recurring posts a mod can make, and this was one of them |

### The owner plays 0.0.10: what a press does, and what a mark says (2026-08-12)

Rows 86-92 come out of the owner playing the shipped 0.0.10 and reading back six things. Five are
the same failure wearing different clothes: **a channel that exists in the code and does not reach
the player.** A rung with no denominator; two buttons whose presses produce identical pixels; a
ladder of five ranks of which one is printed; a right-hand column whose figures sit at the top of
boxes twice their height; a red outline that means "careful" to the author and "glitch" to the
reader. The sixth (92) is a clue that is not on the screen at all.

Quotes, in order: *"still a lot of space. maybe put the logo in there?"* · *"I want the clues
numbered vertically rather than horizontally - reading them column by column somehow feels
easier"* · *"I still don't understand what TIER II means (link two clues)"* - **the third time** ·
*"why does nothing happen when I press the two free buttons?"* · *"I'd like a plaque showing the
ranks, and I'd like the ranks to have emotions"* · *"the undo button gets outlined in red, while
the others just get darker text"* - **the second time**.

| # | Decision | Reason |
|---|---|---|
| 86 | **The tier stamp, third attempt: a word anyone can rank, over the denominator the ordinal never had.** `TIER II / LINK TWO CLUES` becomes **`MEDIUM` / `LEVEL 2 OF 3`**, and the ladder reads WARM-UP · EASY · MEDIUM · HARD. [tiers.ts](../src/client/tiers.ts) swaps `mark`/`gloss` for `mark`/`scale` and gains `technique`, the full sentence the 14-character gloss was compressing; the weight ladder of dec. 77 (outlined `--ink-3` / `--ink-2` at 2px / `--ink` at 4px double) is unchanged and is still the second non-colour channel | Two attempts failed on the same reader and the reason was the same both times, but it was never the *rung*: **an ordinal with no denominator is not a scale.** "TIER II" tells you a ladder exists and refuses to say how tall it is, and a player meets exactly one case a day - they never see the neighbouring rungs to infer it from. Dec. 77 answered the wrong half: it explained what the case would *ask*, in the imperative, so `LINK TWO CLUES` reads as an instruction for the next tap and not as a description of the case. The repair is to say the two things a first-time reader can use with nothing else on screen: a word that is **already ordered in English** (easy < medium < hard is the most conventional difficulty ladder there is, and on a third attempt that is worth more than character), and the fraction (`LEVEL 2 OF 3`) that supplies the scale. Colour is still refused, for the reason dec. 63 gave and dec. 77 restated - it dies in greyscale and under CVD, and in this system it belongs to evidence. The technique sentence is not deleted, it is **rehoused**: it is a sentence, so it goes where there is room for one (dec. 90) and stays in the `title` for a pointer and a screen reader. Checked on a reader with no access to any of the above, given one frame and four questions: the difficulty was named correctly from `LEVEL 2 OF 3`, it was the first thing read - and, the part dec. 77 got wrong, the only thing on the screen read as an instruction was the CTA |
| 87 | **The two-column clue list is numbered DOWN each column**: 1-6 on the left and 7-11 on the right, not 1,2 / 3,4 / 5,6. `grid-auto-flow: column` over `grid-template-rows: repeat(var(--clue-rows), auto)`, with `--clue-rows = ceil(clues / 2)` set by `renderClues()`. The band is written out as `540px-899px` instead of being left to `auto-fit` | Owner's call, and it is the right one: a clue is a sentence you read and come back to, so the eye wants a **column to finish**, not a row to sweep. Two mechanical points. The band is stated because outside it the decision does not exist - below 540px the panel is one column and from 900px it is a 250-330px sidebar - and 540px is exactly where `auto-fit, minmax(260px, 1fr)` used to break, so no breakpoint moves. And the row count is made **explicit** rather than reached with `column-count`: a multicol box that runs out of room grows an overflow column sideways, and this list lives inside `overflow: hidden`, which is how a clue disappears without a trace. With the rows counted there can be no third column. Verified at 11 clues (the worst a daily 🟡 can be) and 15 (the worst in the bank at all) |
| 88 | **No enabled hint rung can be pressed without the screen changing.** The ladder keeps a memory keyed by the board itself: the rung whose sentence is on screen is marked `ON SCREEN` and disabled, a free rung that came back `stuck` is marked `NOTHING HERE` and disabled, the rung being asked shows `ASKING…`, and one move on the board retires all of it. `adviceText()` takes the rung as an argument, so the two free rungs can never print the same sentence, and the slip's rail names the rung that answered (`HINT · STEP 2 OF 3`). The client also learns to phrase the engine's `cross` verdict | *"Why does nothing happen when I press the two free buttons?"* - and nothing did. On a 🟡 board the WEAK model has no forced move (measured in dec. 42: 69 of 100 yellow cases force 0 of 12 cells), so both free rungs answered `stuck`, `adviceText` ignored which rung had asked, and the panel repainted the identical sentence. Two presses, zero changed pixels: from the player's side that is a broken button, whatever the server meant by it. The fix is stated as an invariant rather than as a patch, because "print something different this time" would have left the next identical pair to be found by the next player. Note what it is **not**: it is not a fix for the 🟡 wall itself - that is the server's, and it is landing in parallel - it is the rule that a press is always answered, including when the honest answer is "this step has nothing left" |
| 89 | **The rank is a ladder of five rungs, and the rank emoji come back** (🔎 Detective · 🕵️ Inspector · 🎩 Chief Inspector · 🧠 Mastermind · 🏛 Legend of the Yard). All five are drawn on a rail with the day threshold under each; the rung you hold is framed in `--ink`, the rail behind you is ink and ahead of you `--rule-strong`, and the head names your rank and the next one (`4 DAYS TO 🎩 CHIEF INSPECTOR`). It replaces the RANK cell in the facts strip, so the strip goes back to two cells | Owner's call on the emoji, and it is legal inside the system rather than an exception to it - **this is the line dec. 63 already drew and it lands on the other side of it.** Dec. 63 took emoji out of the *chrome* (the 🟢/🔴 traffic light) and kept them in *content* (🍕 ⌨️ - the puzzle's nouns), because content is the thing the screen is about. A rank is the thing this block is about. It is also not the only channel: the frame, the filled rail and the ink day-figure all carry position, so the ladder reads in greyscale, under every CVD type, and with the glyphs missing entirely. On the substance: `RANK Detective · 2 DAYS TO INSPECTOR` named a rung and a destination and drew neither the ladder nor your place on it, which is a progress bar with the bar left out. `rankFor()` is deliberately not called here - it answers "Detective" for a streak of 0, and an unranked player must not be told they hold the first rung |
| 90 | **The splash's docket is a ruled ledger, and the left column carries the case's technique line.** The docket cells split the column equally and centre their own content (`flex: 1 1 0` + `justify-content: center`, replacing `space-between` on fixed-height cells) and the figures go up to `--fs-xl` / `--fs-lg`; the brief block is vertically centred, and under the one-sentence rule sits `tierTechnique(tier)` - *"Some steps need two clues linked through a value they share."* **The banner is not used** | *"Still a lot of space."* Measured on the frame rather than argued: `space-between` puts every gap **after** a figure, so each cell was a caps key, a number, and a hole down to the next hairline - three holes, which is exactly what the owner described. Splitting the column into equal bands turns the same slack into the leading inside each band, and the hairlines then divide two full bands instead of floating over air. The left column's ~100px lump above the CTA becomes two margins and gains a real sentence, which is the sentence dec. 86 had to move out of the stamp - the wide layout needed content and the stamp needed to lose some, and it is the same content. On the logo: `assets/banner.png` is 2172×724 of dark navy, blue and orange - it is the **pre-D3 palette**, i.e. four hues in the chrome on the one screen whose job is to introduce a system in which colour means evidence - and inside a 190-245px column it renders as a 65px strip whose largest element is the word "Deducto", already set as a wordmark 60px above it. A second mark would be a repeat of the identity and a contradiction of the system in one object. The emptiness was a layout defect, not a missing logo. Two smaller things came out of the cold read in dec. 86 and are in the same row because they are the same screen: `~3 min` became `about 3 min` (at 11px mono the tilde was read as a `<`, turning an estimate into a promise), and above 440px of viewport height the card stops stretching (`max-height: 420px`, centred). Nothing in production renders this document taller than the 320px `regular` slot, so that cap changes nothing that ships - but a cover page stretched to 667px is a title, a button and a hole, and the reader with no context asked whether it was still loading |
| 91 | **Decision 57's one exception is withdrawn: `.btn.warn:hover` is deleted and there is no red anywhere in the chrome, in any state.** Start-over joins the ordinary `.ghost .icon` hover. Destructive intent is announced at the moment it is real instead: `[data-armed]` puts the button in full ink, an ink border and a `--groove` ground for the 2.5s the two-tap confirm is live. The `warn` class is gone from the markup as well as the stylesheet | Owner's report, twice, and both times read as a rendering fault - which is the whole argument: on a rail where every other control answers the pointer by *deepening*, one control that answers by changing **hue** does not read as a louder version of the same thing, it reads as a different thing having gone wrong. Under it is the rule of one red (dec. 64/67): red is the mark made on the file, one meaning per screen, and on the board that meaning is already spent on the contradicted clue. A second meaning does not add a warning, it dilutes the first. And the warning was in the wrong place in time: a hover fires on a button nobody has pressed, while the state that actually deserves a mark - *your next press clears the board* - had no mark on the button at all, only a toast. Why two taps plus an armed mark is enough without a colour: the action is **not destructive in the sense that matters**, because the solve, the timer and the hint count all live on the server (dec. 41) and the board is re-derivable by hand; the cost of a mistaken clear is the marks you have made, which is what the second tap is protecting, and it is now visible on the control that is asking for it |
| 92 | **A fourth density step, `tightest`, for cases above 14 clues** (`padding: 1px`, and 0 under 700px of viewport height) | Found by the screenshot harness while re-measuring the clue list for dec. 87, not by anyone reading the code: the bank holds **one 15-clue case and its tier is green**, i.e. it is a daily. `#clue-list` ran **+10px past its own box at 375×667 and +16px at 360×800** - the two commonest phones - and `overflow: hidden` means the fifteenth clue was simply not on the screen and nothing said so. The density ladder had been cut for the 14-clue tutorial case (dec. 43) and one more row was one row too many. This is the third time this exact failure has been caught by measuring frames rather than reading CSS (dec. 66, dec. 78, here), which is the argument for the harness, not for the fix |

### The owner plays 0.0.11: two states, one layout, and a band that stops short (2026-08-13)

Rows 93-95 come out of the owner opening 0.0.11 after solving the case. Two of the three are the
same failure as dec. 90 and dec. 92 wearing different clothes: **a box measured against one case
and shipped for all of them.** Dec. 90 divided the splash's right-hand column into equal bands and
grew the figures to fit three of them; a solved player has five. Dec. 92 found a clue list one row
too long. Both are silent, and for the same reason - `html`/`body` are pinned to `overflow:hidden`
(dec. 74), so a box that does not fit neither scrolls nor clips: it paints over what is under it.

Quotes: *"the numbers run into each other"* - `YOUR STREAK` lying across `04:59` on the solved
splash · *"the brown fill breaks off before the points column; it should run the full width of the
row"* · *"it isn't clear why 0 of 12 your board is here. we should think about what else we can
show on this preview"*.

The third is not a defect report and is answered as a question about **what a line is for**, which
is the same question dec. 80 asked of the result sheet: a row costs the same whether or not it
says anything, and on the first screen it costs the most.

| # | Decision | Reason |
|---|---|---|
| 93 | **The splash docket's bands are sized from how many of them there are, and a band may no longer be shorter than its content.** `renderDocket()` writes the count onto the docket as `data-n`; splash.css keys `--dv-fs` / `--dv-gap` / `--dn-fs` off it (26/21/17px at three, four and five bands, one notch down inside a 320px slot and another below 300px), and `min-height: 0` comes off `.dcell` | Dec. 90 was right about the mechanism and measured it at one value of N. `flex: 1 1 0` splits the column into equal bands, which is what turns slack into leading - and `min-height: 0` lets a band be **shorter than the key and figure inside it**, which is what turns a shortage into an overlap. The arithmetic was there to be done: a band carries 4 + 11 (key) + 5 + 28.6 (a 26px figure at 1.1) + 4 = **~53px**, and the docket's column in the 320px feed slot is ~220px. Three bands need 158 and fit; five need 265 and do not, so each is ~9px short and every key after the first is painted 17px into the figure above it. Measured on the frames rather than argued: **18 of 72 two-column splash frames carried a collision, worst 17px, and 16 carried a `.dcell` overflowing its own band; after, 0 and 0.** Two smaller notes. The overlap was invisible to the track-06 harness because the docket is a grid item stretched to the card's content edge - *it* never overflows the card, and the selector list did not name `.dcell`; the harness now measures the bands and, separately, reports any two stacked cells whose text boxes intersect. And the count is written onto the element rather than discovered with `:nth-last-child` quantity selectors, because the number of bands is data the client already has - a layout should be told what it is drawing |
| 94 | **In the ruled ledger the ROW is full-bleed and its text is inset, not the other way round.** The 12px gutter moves off `.st-list` and the 4px off `.lb .b-row` onto a single `--row-pad` set by the list and consumed by `.b-row`; `.b-row.you` drops its extra `padding-left` and `.b-you .b-row` sets `--row-pad: 0` | The owner's row was `1 u/ma9leb YOU … 830 pts` on a board where he is the only entry, and the `--groove` ground ended on the `s`. Measured on the rects: the row started **12px** inside the panel and ended **12px** short of it (24px with a desktop scrollbar), while the value's right edge was **exactly** the row's - so the fill terminated on the last glyph of the figure it exists to highlight. It is not an inset that is wrong, it is *whose* inset: on that screen the recessed meta line and the tab rail's rule are both full width, so the marked row was the one band on the page that stopped short, which is precisely how it reads. Moving the padding inside the row also fixes the hairlines, which had the same 12px and divided rows that were narrower than the sections above them. After: **left 0, right 0**, with the value now 12px clear of the edge. What remains is a desktop scrollbar's own 12px on a list that actually scrolls - the webview a phone renders this in uses overlay scrollbars and takes none |
| 95 | **`0/12` is not printed, and what the docket carries is the player's state.** Before the first move the board cell is keyed to the case (`TODAY'S BOARD`) and the twelve squares are the whole value; `FASTEST` is dropped once you have solved and `NEXT CASE` promoted above the room's figures. A docket is now two to four cells, never five | Owner's call on the zero and he is right about it, but the reason is worth stating because it generalises: **`0/12` is the same value for every reader who has not started.** It cannot differ, so it carries no information, and it spends the most expensive line on the product's first screen phrasing that non-fact as a verdict on the reader. The squares survive the cut because they are countable, which is the only thing the fraction was adding, and because they are still the cheapest answer to "what is this game" (dec. 84) - what changes is who the cell is about. The other two moves come from one rule applied to all three states: *before* you play the docket answers "is this worth three minutes, and is anyone here" (the board, the room, the benchmark); *after* you play it answers "what did I get, and when is the next one" (your time, your streak, the countdown, the room). `FASTEST` is a second time figure beside your own with no rank to read it against, and the sheet one press away carries it with the whole day's board around it; the countdown is the only forward-looking line on the screen and the only figure that sheet does **not** carry, which is why it takes the phone's third slot. What this decision does not touch, and should: the CTA's sub-label prints `SOLVED IN 04:59` 40px under `YOUR TIME 04:59`, and `5 OF 12 CELLS DEDUCED` under the same fraction - the duplication dec. 80 removed from the result sheet, on a smaller screen. It is left for the owner because the button is not what he asked about |

### The first screen is a shop window, not a lid (2026-08-15)

Rows 96-99 come out of the owner reading the live 0.0.14 splash with five players on the board.
Three of them are the same failure and it is not a layout one this time: **a screen that answers
the question the code found easy instead of the question the reader is asking.** "How many solved
it today" was the count that was one Redis call away, not the count anyone wants; the benchmark
was withdrawn because a *sheet behind a button* carries it, from a reader who could not find the
button; and the standings were put behind that same button by a decision whose premise had already
been deleted eleven rows earlier.

Quotes: *"stupid metric, solved today. why only today? maybe how many solved in general?"* ·
*"I'd like to see buttons on the main menu, to see the leaderboard - and you can't bloody get to
it. You have to open a case to open the leaderboard."*

The thread through all four: on the first screen, **a line has to earn its width against the
reader's question, not against how cheap it was to compute.**

| # | Decision | Reason |
|---|---|---|
| 96 | **The room's cell names a population that is worth naming, and on a young game that population is the community rather than the day.** From **ROOM_MIN = 10** solvers up the cell is `SOLVED TODAY` and carries `today.solvers`; below that it is `DETECTIVES` and carries the new `today.solversAllTime`; with neither it is words, as before. One rule, all three player states | Two defects in one cell, and only the second was reported. **The label counted a different population from the one it named:** `solvers` was `zCard(lb:{postId})`, i.e. the people who closed *this case*, and publishing has no same-day guard - on **2026-08-14 two cases went out and the two splashes read 5 and 1**, both under the word "today". That half is fixed at the source, in the same pass: the endpoint now reads the day board `lb:day:{day}`, which is the key the standings, the funnel and the streak already use, so the word counts the day it names. **What a label cannot fix is the size of the number.** On a young game the day's count is 0 or 1, and a first visitor reads `SOLVED TODAY 1` as *nobody is here* - on the one screen whose whole job is to say the opposite. So below ten the cell carries a different population under a different word. The threshold is not invented for it: it is the rule this codebase already runs the histogram and the percentile on (dec. 37), *a figure is printed where it means something*. Ten rather than the fifty a percentile needs, because the two gates answer different questions - a percentage is a claim about a distribution and needs a sample to be one; a headcount is only being asked whether it reads as a room. `solversAllTime` is free and exact: `lb:alltime` already carries one row per player (`writeSolve → zIncrBy`), so it is a count of rows rather than a counter anyone maintains, correct retroactively for every solve since the leaderboard shipped, and never smaller than the number it stands in for |
| 97 | **`FASTEST` comes back for the solver, second in the row, right beside `YOUR TIME` - and dec. 95's "a docket is two to four cells, never five" is withdrawn with it.** When the day's best time is your own the cell reads `FASTEST / Yours` instead of printing your figure twice. Solved order: your time · fastest · next case · your streak · the room | Owner's call, and the interesting part is *which* argument it kills. Dec. 95 dropped the benchmark because "it is one press away with the whole day's board around it" - an argument about **reachability**, made about a button the same reader then could not find (dec. 98). An argument that a reader disproves by behaviour does not get to stand on its wording. Placement is the substance of the row: **adjacency is what makes a second time figure a benchmark** rather than a stray one - `YOUR TIME 04:57 · FASTEST 02:41` is a comparison, the same pair two cells apart is two clocks. One edge had to be got right rather than discovered later: when you hold the best time the benchmark **is** your own figure, and 04:57 printed twice 40px apart reads as a rendering fault, not as the fact that nobody has beaten you - so the cell names whose it is (`>=`, not `==`: a tie is still yours). The count goes back to five, which is safe only because dec. 93 sized the bands for five and this was re-measured on the frames rather than assumed. The ordering rule is restated because the phone strip draws **three** cells and the rest are simply not there: order by what only *this* screen says. The countdown keeps its place ahead of the streak for dec. 95's own reason - the sheet behind the button carries the streak and not the countdown - and the room's headcount goes last once you have solved, because you are in it now and that sheet prints it with your rank beside it |
| 98 | **The standings are reachable from the feed. `Standings` is a secondary button beside the CTA, and dec. 84's reason for keeping one off the splash is withdrawn.** The trip is `requestExpandedMode(event, "game")` plus a one-shot note in the origin's storage ([handoff.ts](../src/client/handoff.ts)), which [main.ts](../src/client/main.ts) consumes on boot - and **`/api/daily` is not called on that path** | Dec. 84 kept the standings off this screen because "they live in a separate post and the splash has no URL target"; **dec. 85 deleted that post** and dec. 75 had already made the standings a screen inside the game, so the premise has been gone for eleven rows and the button was still missing. Still exactly one *filled* button, so the hierarchy the spec asks for is intact. The mechanism is the work, and it is all constraint: there is one expanded entrypoint (the board); `requestExpandedMode(event, entry)` takes an entrypoint **name** and the host builds the destination URL itself out of `devvit.json` (`@devvit/client/effects/web-view-mode.js` reads `devvit.entrypoints[entry]`), so the splash cannot hang a hash, a query or a payload on it; and expanding **replaces the document** (dec. 46), so memory is not a channel. What the two entrypoints do share is an **origin** - both are files under `post.dir` - so the note travels in that origin's storage, stamped, consumed on read, and ignored after two minutes. **Two stores, because the reload has two shapes:** a same-context document swap keeps `sessionStorage`; a client that tears the web view down and builds a new one does not, and there `localStorage` is what carries. Every access is guarded - a web view sandboxed without `allow-same-origin` throws on the first property access, and `localStorage` has been called unreliable in this web view once already (dec. 52) - and a handoff that does not arrive is **not an error**: the board opens, with the standings one tap away in its control rail, i.e. the button degrades into the Play button. The half that is a decision rather than plumbing: **the daily is not fetched on this path.** `/api/daily` is the request that stamps `startedAt` and consumes the first-run tutorial flag - it is the request that *starts your run* - and the press that asked for a table did not ask for a run; the load waits for "Back to the board". **Verified rather than reasoned** (`plans/assets/proto/09-splash-hub/handoff.sh` (internal), reproducible): a real headless browser presses the real button on the built `splash.html`, points the same browsing context at the built `index.html` - a genuine same-origin document swap - and reads back what the next document did. All pass: the note is written to both stores; the board comes up on `standings`; **`/api/daily` calls on that path: 0**; the note is consumed on read; with the session note wiped **by hand** - which is the fresh-web-view case exactly - the `localStorage` leg still lands on the standings; and an open with no press behind it lands on `game`, with `/api/daily` called once |
| 99 | **The CTA's sub-label stops repeating the docket.** `See results` and `Continue` lose theirs; `Open the case file · ABOUT 3 MIN` keeps its | Dec. 95 found this and left it: `SOLVED IN 04:59` printed 40px under `YOUR TIME 04:59`, and `5 OF 12 CELLS DEDUCED` under the same fraction - the duplication dec. 80 struck off the result sheet, on a screen a fifth the size. Two things make it worth closing now rather than later, and both are consequences of the rows above. Dec. 97 puts a **second** time figure in the docket, so a solved card was about to print 04:59 three times inside 120px; and dec. 98 gives the CTA row a second button, which the primary pays for out of its own label width. `about 3 min` is not a duplicate of anything on the card - it is the estimate a stranger is actually deciding on, and dec. 90 already had to repair how it reads |

### Two runs, one word: what agent R's statistics audit left to the client (2026-08-15)

Row 100 is the client half of the audit in `plans/assets/09-stats-audit.md` §7. The server side of
it landed in the same pass and is why row 96 above reads the way it does. Everything here is text
and markup: **the figures were right and the words around them were not.**

| # | Decision | Reason |
|---|---|---|
| 100 | **A streak is two numbers and they never share a word, the rank ladder is the server's answer, and two copy lines stop naming the wrong scope.** The result strip goes to three cells - `HINTS · CURRENT STREAK · LONGEST STREAK` - and the board HUD's `Streak` meter now reads the run in progress rather than the best ever; `renderRanks()` takes `results.rankState` instead of recomputing it; `You're the Nth detective to close it **today**` becomes `to close **this case**`; `#N opens in 0h 00m` becomes `#N opens soon`, on the result sheet and in the splash's `NEXT CASE` alike; and the standings' `ahead of N%` moves from `SMALL_N = 10` to **`PCT_MIN = 50`** | Five findings, one shape: **a figure printed under a word that describes a different thing.** (1) `Streak` sat over `best` in the HUD and over `best` on the plaque while the meter beside it counted today - and the STREAK board one tap away is titled `Longest streak`, so the vocabulary was already there to borrow rather than invent. Three cells reverses the "back to two cells" clause of dec. 89, and the reason is the opposite of dec. 89's: RANK left because the ladder draws it *better*, the current run arrives because the ladder cannot draw it *at all*. (2) The ladder computed `next.min - streak` off one number, so a player whose best is 12 and whose run has broken to 1 was told the next rung was two days off when it is thirteen; `rankState()` is the server's shared answer - the same one the subreddit flair reads - and its `label: null` is the state the client must not guess at, because "Detective by default" is a rank nobody earned. (3) `solveOrder` is the finishing position on the **case** (`solved:{postId}`) while `total` beside it is the **day**: the number was right and "today" was not, and on a two-case day "the 3rd detective to close it today" is two different people. (4) `nextOpensInMin` floors at zero, so an overdue case printed a countdown counting down to a moment in the past. (5) The percentile had **two thresholds a factor of five apart on two screens one press apart** - the card gating its hero line on `hasHistogram` (50) and the pinned standings row on 10 - and the softer of the two was the one a stranger meets first. Dec. 37 already states the number: a percentile and a distribution appear together, from 50 up |
| 101 | **The result sheet's solution table is built from a snapshot of the board that closed the case, not from the live one.** `solvedGrid` is taken wherever `lastResults` is, and `showResult()` reads it; an undetermined cell is skipped rather than dereferenced | Found by the screenshot harness on a fixture whose saved grid was not a solution, and it is reachable by a player in two presses: **`Play this case again` clears the board and deliberately leaves the Results button on screen "to reopen the recorded result"** - and that button rebuilt the table out of `grid`, which is now empty. `effectiveValue` answers `null` for an undetermined cell, `f[0]` threw on it, and the throw landed *between* the sheet being filled in and `body[data-view]` being set - so the screen never opened and nothing said why. A button that does nothing is the same defect dec. 88 is about, arrived at from the other end. The snapshot is also the honest object: the client never receives the solution (dec. 41), it only ever holds the grid it solved with, so that grid *is* what the second face of the sheet has to keep |
| 102 | **Cases closed is a cell of its own, and on the splash it is the cell the streak leaves behind.** The result strip gains `CASES CLOSED` (drawn only above zero, ordered `HINTS · CASES CLOSED · CURRENT STREAK · LONGEST STREAK` so the two runs share a row when four cells wrap to two); the splash docket carries **one** cell about the player, and it is `CURRENT STREAK` while a run is alive and `CASES CLOSED` once it is not. The STREAK board's note gains the sentence *"Any case counts, including one from the archive."* | Agent T's streak repair (`src/server/streak.ts`) settles what a streak IS - consecutive calendar days on which you closed at least one case - and the same paragraph names what the player in the bug report was actually counting: **cases**, a different question that must therefore never borrow the streak's word (dec. 100's rule, applied to a third number). Two things follow that are not just "print the new field". First, the gate: `casesSolved: 0` means *nothing recorded yet* rather than *no cases*, because the counter is seeded going forward - so it is drawn only above zero, the convention `streakCurrent` already uses. Second, and the reason the splash needed thinking about rather than a fourth cell: T's repair makes `streakCurrent: 0` a **fact** ("the run is over") instead of missing data, and the docket hides that cell - which left a veteran who missed a day looking at a feed card with nothing about them on it at all, while the figure they were looking for was precisely the one the bug report was about. So the cell swaps rather than doubles: the phone strip draws three cells and one line about you is what this screen can afford. It never swaps on the solved card - you have just closed a case, so the run is alive by definition. The archive sentence is on the STREAK board because that is the line whose job is to say how a figure is arrived at (dec. 83), and it is behaviour a player cannot guess and that is in their favour: on a day the sub published nothing, an old case now keeps a run alive where it used to be the only way to lose one |

### Three worlds, and a card that never said which one it was in (2026-08-16)

Rows 103-107 come out of one sentence from the owner, and it is the sentence four tracks had been
circling: *«в целом не проработано у тебя что показывать какие статистика когда где и как
правильно»* - followed by the diagnosis that made it actionable: *«надо правильно понять, как
связать текущий кейс, любой кейс — со всей статистикой игрока в целом. что показывать на кейсе,
именно по нему, а что именно за всё время участника игры в сообществе»*.

Every figure in this game had been decided one at a time, in answer to one complaint at a time.
Each was defensible alone. Together they produced this, on the solved feed card:

```
YOUR TIME 05:20 · FASTEST 01:54 · NEXT CASE 8h 53m · CURRENT STREAK 4 · DETECTIVES 8
└──────────── this case ───────┘  └── the series ─┘  └──────── you, all time ─────────┘
```

**Five cells, three subjects, one typeface, one weight, one rhythm, and nothing saying so.** The
whole spec is [docs/11-stats-ia.md](11-stats-ia.md): every number, on every surface, in every state,
with the reader's question it answers and the world it belongs to. The three complaints the owner
listed were not three tasks - they were three symptoms of the missing axis.

| # | Decision | Reason |
|---|---|---|
| 103 | **Every figure belongs to one of three worlds - ① this case, ② the series, ③ you here all time - and the arrangement says which.** Cells of one world stand together and are never interleaved; the boundary BETWEEN worlds is drawn one weight heavier than the boundary inside one (`--rule-strong` against `--rule`). Applied to the splash docket (① then ③, one boundary, two or three cells - never four or five) and to the board HUD, where the streak meter moves out from between the two case meters. **The splash's room cell counts THIS CASE** (`zCard(lb:{postId})`), in every state, under `CLOSED IT`; `ROOM_MIN` and dec. 96's two-form cell are revoked, `FASTEST` appears only on the solved card, and `SOLVED TODAY` and `DETECTIVES` leave the splash | Two defects, and the second is the reason the first kept coming back. **The reported one:** `DETECTIVES 8` on a card glued to one case was `zCard(lb:alltime)` - everyone who had ever closed anything. The owner: *«надо показывать сколько всего детективов решили именно этот кейс»*. He is right, and dec. 96 is the thing that was wrong: it gave one cell two subjects and switched between them on a hidden threshold, so a reader watching `SOLVED TODAY 12` become `DETECTIVES 8` had no way to know the population had changed under them. That is the "one word, one number" rule failing in its purest form, and the threshold goes with the swap - `ROOM_MIN` gated a change of **subject**, not a change of precision, which is what every other gate in this codebase does (dec. 37). **The unreported one:** the card had no way to say which world a cell was in, so *every* cell looked like it was about the case - which is precisely how the complaint got phrased. The fix cannot be a label per cell (noise on a 320px card) so it is the arrangement: order plus one heavier rule, no colour, no extra word, in the vocabulary the docket's own frame already uses. The budget makes it a real decision rather than a tidy-up: the feed slot draws **three** cells, so choosing which world fits is the design. Before you play, ① board + ① closers ‖ ③ your run - and a stranger, who has no ③ cell, gets two cells of one world. After you solve, ① your time + ① fastest ‖ ③ your run, with world ② moved out of the strip entirely (row 104). The strip is now **complete on a phone in every state** rather than truncated at three, which is also why dec. 93's four- and five-band type ladder could be deleted rather than kept as rules nothing can select |
| 104 | **The splash's brief changes world with the player's state, and that is where world ② lives.** Before you play it is the rule of the game plus what this case asks of you (①); once you have closed it, it is the countdown plus what the vote is doing to the next case's level (②), with the raw tally on the wide layout. `NEXT CASE` stops being a docket cell. The result sheet's vote block gains the same verdict line, from the same server function | Owner: *«если уже решил, интересно посмотреть кто за что проголосовал, какой следующий кейс будет тяжелее или легче»* - and the product argument is stronger than the request: **a solver is no longer this case's player, they are the next one's audience**, and "what will tomorrow be" is the only thing still holding them here. The data existed and was unreachable - the vote lived only on `/api/daily`, which is the request that *starts a run*, so the feed card could not have it without becoming exactly the side-effecting endpoint it must never be. Three things had to be got right rather than discovered later. **It is a sentence, not two more cells** - which is what buys row 103 its clean split, because a countdown and a vote are world ② and would otherwise stand inside a world ① strip. **The verdict is gated on what the vote can DO**: below `VOTE_MIN_TOTAL = 5` it moves the difficulty by exactly nothing, and this subreddit collects two - so a three-segment bar with printed percentages was showing a decisive-looking picture of a decision that had not been taken. And **`moved` is separate from `verdict`**, because at either end of the ladder a decided "Harder" still changes no level, and a screen printing the verdict alone would promise a step the publisher then declines to take. One definition of "decided" (`voteVerdict()`) now serves the publisher, the card and the sheet, so no two screens can promise different tomorrows |
| 105 | **The result sheet is ① → ③ → ②, each block homogeneous.** The hero's comparison becomes case-scoped (`2nd fastest of 7 on this case`, or a percentile at `PCT_MIN = 50`, or the finishing-order fallback with no row); `HINTS` stops being a fact cell and becomes ` · 1 cell revealed` on that line; `CASES CLOSED` stops being a fact cell and becomes the kicker `CASE #47 · YOUR 9TH`; the strip is left as `CURRENT STREAK · LONGEST STREAK`, which is world ③ and nothing else | Three moves, one rule. **The hero line was the splash's bug one press away**: it switched between a percentile over the DAY and a finishing order over the CASE depending on `hasHistogram` - one sentence, two subjects, chosen by a threshold no reader can see. Both branches now measure one thing (your place among the people who closed this same puzzle) and the threshold chooses only how precisely to state it, because a rank is exactly true at N = 3 while a percentage is a claim about a distribution. The day is not demoted - it keeps every figure inside the standings block that is *titled* with the day, which is the rule that lets four boards share the word `detectives` honestly. **`HINTS` was a word wider than its counter** (agent R's finding: rungs 1 and 2 are hints too, and free, and are not in that number); as a qualifier beside the figure it qualifies, it is both narrower and exactly true. **`CASES CLOSED` is not deleted, it is promoted**: dec. 102 put it on this sheet a day earlier and was right that the number the bug report was counting has to be on screen - but as a cell it was world ③ standing inside a world ① strip, and as a kicker it answers a question neither world answers alone (row 107). It also survives as a cell in the file that owns world ③ |
| 106 | **World ③ gets a surface of its own: `Your file` (`GET /api/me`, `body[data-view="me"]`).** Cases closed, days solved, since, current and longest streak, best time, points, place-of-N, the rank ladder, and the last fourteen days. Reached from your own row in any standings list and from a button under the result sheet's ladder. Other people's files: no | **This is the root, and it is why every previous fix bounced.** The case is a post and the day is the feed; "you here" had *nowhere*, so it was smeared across screens belonging to a case and to a day - a streak in the corner of a case card, points in someone else's table, a rank in a subreddit flair. **A figure with no home takes the nearest one**, and `DETECTIVES 8` on a case's cover page was that, exactly. Rule 103 can mark the boundary but cannot fix the budget: with three cells in the feed slot, every world ③ figure competes directly with the case's own. The alternative was to spread world ③ further and hope the rule carried it, which also leaves `user:{username}` - `solved`, `totalPoints`, `bestTimeSec`, `firstSeen`, written on every solve since the leaderboard shipped - read by nothing, forever. Nothing here is a new measurement and nothing is a new counter: it is one read-only endpoint over keys that already existed, and it was already planned (`plans/04-leaderboard.md` (internal) §D.4, *"Дёшево (данные уже есть)"*) and never built. It reuses the standings' frame deliberately - the two screens are the same act of looking something up in the same archive. Two details that are decisions rather than plumbing: `CURRENT STREAK` is drawn **at zero here and nowhere else**, because this is the surface that owns the run and "it is over" is something it exists to say, while everywhere else a zero means "not recorded yet"; and `PLACE 3 of 8` is the one figure on the screen that is not about the reader, printed as a denominator, which is what stops it becoming another `DETECTIVES 8`. Other people's files are refused not for privacy theatre - the boards already publish name, time, points and streak - but because what is public about a player who never opted into a profile is a product question of a different shape, and the extension is one parameter when somebody asks |
| 107 | **Three cross-world links, and only three: `CASE #47 · YOUR 9TH`, the `Your best` tag on the hero time, and `FASTEST 02:41 · You · of 7`.** The `FASTEST` cell prints the figure with a filled `You` tag instead of the word `Yours`. The board epilogue's two figures are re-read live from `lb:{prevPostId}` instead of the snapshot frozen into `postData` | Separation alone would leave three sealed rooms, and the strongest lines in the product are the ones that need two worlds at once: *what does this case mean to me*, *is this the best I have ever done*. A link that is everywhere is decoration, so there are three and each answers a question neither world answers alone. Both gates on the personal best are about honesty rather than arithmetic - a **replay** re-submits a recorded time and would re-announce an old solve as news every time a solved post is reopened, and a player's **first** close is trivially their best, so a record needs a record to beat. The `FASTEST` cell is the owner's third complaint and the most instructive: the shipped code's reasoning was right (`04:31` printed twice 40px apart reads as a rendering fault) and its conclusion was wrong - it withheld the one figure the cell exists to state. The ledger had solved the same problem one screen away with a filled `You` tag on your own row, so the answer was to borrow the idiom rather than invent a third one; `of 7` beside it is the size of the field the benchmark is the best of, which is what turns a time from nowhere into "best of seven". The epilogue is the same failure in miniature: two figures frozen at publication under a sentence in the present tense, so every solve after the next case went out was invisible to it |

### The harness was measuring a layout nobody has (2026-08-17)

Rows 108-111 come out of one fact, checked before anything was fixed:

```
fc-list on the machine every screenshot has ever been taken on
  Iowan Old Style  NO      SF Mono   NO      Segoe UI  NO
  Palatino         NO      Menlo     NO      Roboto    NO
  Georgia          NO      Consolas  NO      Arial     NO
```

**Not one family in `--font-serif` or `--font-sans` exists there.** The stacks fell through to
whatever fontconfig aliased - DejaVu, chosen by nobody - while the device serves SF Pro, SF Mono
and Iowan Old Style. So every frame this project has ever shot, across tracks 03 through 11, was a
measurement of a layout no player has seen; and every pixel budget in `game.css` (the density
ladder, the short-viewport blocks, "measured at 375x667, +10px past the box") was hand-fitted to
that accident. Track 11 reported 472 clean frames the same week the owner photographed three text
overlaps on his iPhone.

Two failures, and the second is the worse one. **The fonts were wrong** - and **the probe that
would have caught it was never read**: a DOM measurement has existed since track 06, posting
`clipped` / `spill` / `over` to a log on every frame, and no command has ever looked at that log.
Track 11's own log held 18 clipped boxes and 8 real overflows while its report said the matrix was
clean. A log nobody reads is a diary, not a check.

| # | Decision | Reason |
|---|---|---|
| 108 | **The harness forces the font, shoots the matrix under TWO profiles that bracket the device, and identifies what actually rendered by its own metrics.** `?f=narrow` is Liberation Sans / Liberation Mono / P052, `?f=wide` is the DejaVu family; every frame reports the width of a probe string, and `check-frames.py` compares it against the advance widths read out of the font FILE with fontTools. A frame that cannot name its font is not evidence and the run fails | The honest position is that this machine **cannot** render the device's fonts - SF Pro and Iowan Old Style are Apple's and no free metric clone exists - so the choice is between pretending and bracketing. Measured (`measure-fonts.py`, in em): the vote sentence runs 25.7 in Liberation and 29.3 in DejaVu (**+14%**), the long case title 19.8 and 24.9 (**+26%**), and `normal` line-height across the available faces runs 1.000 to 1.200 (**+20%**) - which is what makes text print over text, since a line box shorter than the font's own ascent+descent lets glyphs paint outside it. A layout that survives both ends survives anything between them, and the device sits between them. One happy accident worth stating: **every monospace face advances exactly 0.600em per character, and so does SF Mono**, so the docket's keys and figures were always measured correctly - the whole risk was in sans and serif, which is exactly where the three reported bugs were. Identification is by metrics rather than by asking the browser (no engine exposes the resolved family) and rather than by comparing against a family that does not exist (tried first, and unsound: an unknown family falls back to the browser default, which on this box IS DejaVu Serif, so forcing DejaVu Serif looked identical to the fallback). 1.5% of tolerance covers GPOS kerning, which the browser applies and `hmtx` does not; the load-bearing test is that the measurement is nearer this profile's font than the other's, and the two profiles are 9-27% apart |
| 109 | **The probe is a gate, not a log.** `check-frames.py` exits non-zero on any clipped box, spill, container overflow or text-over-text, and `shoot.sh` runs it per profile and fails the shoot. It also fails on *no reports at all* - a matrix with nothing behind it is the failure mode that produced 472 frames of a dead server. Text-over-text is new and is the class the owner photographed; deliberate truncations (`-webkit-line-clamp`) and deliberate scrollers are whitelisted, and a scroller that actually hid content is reported as a note rather than a failure | The instrument was already there and pointed at the right things; what was missing was a command that reads it and a build that cares. Three whitelist repairs were needed before its signal could be trusted, and all three were found by reading track 11's own log: the case title's two-line clamp was reported as a clipped box on nine frames, `.me-body` was not known to be a scroller, and the new `.clue-list` scroller had to stop counting as an overflow. The overlap threshold is a **fraction of the smaller line** rather than a flat pixel count, because two lines set at `line-height: 1` in a font whose ascent+descent exceeds 1em overlap by construction - ordinary typography, ~2px on the tier stamp's 11px rows, and a flat tolerance called every one of them a defect. Three ways the harness could produce nothing while exiting 0 are now refused up front rather than diagnosed again: a dead fixture server (every page then waits forever on the `/hold` image), a `--screenshot` path outside `$HOME` (snap firefox discards it), and a stale profile lock (snap firefox is single-instance, so an overlapping launch attaches to a dying one, prints "already running", writes nothing - they piled up to nine live browsers and zero frames before this was understood) |
| 110 | **The matrix shoots the heaviest case of every density band, not an arbitrary member of it.** `game-10` / `game-12` / `game-14` / `game-15` join the states, drawn from real bank cases | `main.ts` picks the band by clue count - >14 tightest, >12 tight, >10 dense - and the old matrix shot **11 and 15**: the lightest case of `dense`, the only case of `tightest`, and nothing at all from `tight`. The bank holds 10 twelve-clue cases and 17 fourteen-clue cases and **the harness had never rendered one of either**. Twelve is the heaviest `dense` case and it is exactly the case the owner opened. A band is only tested by its worst member |
| 111 | **A box that cannot fit must clip or scroll; it may never paint through.** `.clue-panel` takes `overflow: hidden` and `.clue-list` becomes a bounded scroller; `.r-body` on the result sheet takes `overflow-y: auto`. The "nothing scrolls" rule of the design gains its second and third exceptions, after the standings list | This is the same defect twice, and it is the mechanism behind two of the three photographs. Both are flex items with the default `flex-shrink: 1`, so a short viewport squeezes them **below their content height** - and with `html`/`body` pinned to `overflow: hidden`, a box that does not fit neither scrolls nor clips: it paints through whatever is under it, in silence. Clue 12 went under the board's COAT/TIME/ITEM header; the vote's verdict line went under `Full solution` and `Play this case again`. Every previous answer to this class was a pixel budget, and every one of those was fitted in the wrong font, which is why the class kept coming back. `overflow` is the answer that holds whatever the metrics do. The scroller is what makes the clip safe: a clue you cannot read is a case you cannot solve, so the content has to remain reachable - and the density ladder still does the work of making the scroll rare, it is simply no longer the only thing standing between a long case and an unreadable screen |
| 112 | **`FASTEST` names WHO holds the best time instead of how large the field was.** `of N` is deleted; the cell carries the time, a filled `You` tag when it is yours, and otherwise `u/name` on its own line, clipped with an ellipsis. `/api/preview` gains `case.fastestBy` | Owner's call - *«можно вообще имя писать, чтобы все видели лучшего на посте»* - and he is right on both counts. It **collided**: on a 375px phone the band is ~93px, and the figure plus a tag plus `of 7` do not fit on one line, so the field size ran into the neighbouring cell and `4` and `7` read as one number. And it was the **weaker fact**: a headcount says how many you beat, a name says who to beat, and it is the name that makes a post feel like a room rather than a scoreboard. Its own line rather than beside the time, because a Reddit username runs to 20 characters and there is no width to share; clipped rather than wrapped, because a second line grows the band and a band that grows paints its neighbour's key over its own figure (dec. 93). Nothing is published that the boards do not already publish - `lb:{postId}` members ARE usernames, and every standings row prints one |
| 113 | **The vote is a picture, and it is drawn on every case - including the ones where it decides nothing.** The percentages move INSIDE the bar's segments (a segment under 18% keeps the shape and drops the label); the separate text legend is deleted. On an archive case the block draws the distribution and states, on screen, that this case is closed and the live case's vote is the one that sets the level | Two complaints, one block. *«оно ужасно выглядит, может распределение вместо конкретного количества текстом, хочется визуально видеть»*: there was a bar AND a line of prose under it saying the same three numbers, so the picture and the caption competed and the caption won on weight - `SOFTER 0%` spent a third of the row on nothing. One object now carries both the shape and the figure. The second half, *«не на каждом кейсе видно эти данные»*, was read as inconsistency and it was: docs/11's own rule (an archive case's vote steers nothing, so claim nothing) is correct about the VERDICT and was silently applied to the DATA as well, so some cases showed a vote and some showed nothing with no reason given. How this case's solvers voted is a real answer even when it decides no level - what must not be claimed is the consequence. So the distribution is always drawn and the screen says which of the two it is. Colour is still not a channel: solid / hatched / hollow, unchanged (spec §0) |

### What the owner saw on 0.0.20 (2026-08-17)

| # | Decision | Reason |
|---|---|---|
| 114 | **The standings block on the result sheet shows WHOLE rows and does not scroll.** `trimToFit()` is called on it after paint, `.lb` goes from `overflow-y: auto` to `hidden`, and the note counts what is on screen rather than what was sent | The block opened on the bottom half of row one - a stub of a name above the first full row. Its own CSS comment already said what it should do (*"four rows is what the sheet can spare; the way to the rest is a press, not a scroll the page is not allowed to have"*) and the CSS said `overflow-y: auto` anyway, so the comment and the code disagreed and the code won. The clamp is `4 × clamp(26px, 4.4vh, 36px)` - four NOTIONAL row heights, which is not a whole number of REAL ones, so the box cut a row in half wherever the two differed. **The guarantee already existed and had lost its caller**: `trimToFit()` measures the rows that actually fit and removes the rest, it has been exported since the ledger was written, and nothing has called it since `board.ts` was deleted (dec. 85) - the function survived the file, the call did not. Measuring beats arithmetic here for the same reason it did everywhere else in track 12: a row height computed from a token is a row height in a font this machine does not have |
| 115 | **An archive case has no vote block at all.** `next === null` (the server's answer that this post's tally steers nothing) draws neither buttons nor bar; the countdown to the next case stays | Three versions of this have now been wrong. It offered three buttons that changed no difficulty; then - track 12's first pass - it showed the distribution with a sentence explaining why the buttons were gone. The owner's read of the second (*«нужно ли голосование на старых кейсах? кажется что нет»*) is this document's own R2 applied honestly: a cell answers a question the reader has **here**, and nobody reading a case out of the archive is asking how its solvers voted on a level that was set weeks ago. An explanation of why a control does nothing is still the control taking the room. This revokes the archive clause of dec. 113, which argued the distribution was worth keeping as a record - it is a record nobody asked for |
| 116 | **The day tab and the day board's title say TODAY only when the day IS today; on an archive case they carry the date.** `readBoard` gains `live`, compared against the sub's newest publish (`lt:lastPostDay`), not against `currentDay()` | The tab said `TODAY` over rows dated three weeks earlier while the meta line directly under it printed `2026-07-27 - ranked by time`, so one screen contradicted itself and the reader had to decide which half to believe. "Current case" was the owner's suggestion and is the one option that is *also* wrong: the board is scoped to a DAY, and on 2026-08-14 two cases went out on one day, so a board titled with a case would misname itself exactly when it matters. The date never lies. The comparison is against `lt:lastPostDay` and **not** `currentDay()`, which was the first attempt and is a tautology: `currentDay()` resolves from `postData.date` when read inside a post, so `date === currentDay()` is true on every post ever published and says nothing. The day the sub is *on* is the day of the newest publish |
| 117 | **The week's numbers get a mod-menu item that emits ready-to-paste text; the app never posts it.** `/internal/menu/week` assembles the top three by points, the fastest single solve, the counts and the period, for last week and this week | The owner wants a weekly "player of the week" post and the copy belongs to another track; the question here was whether the data exists, and all of it does - `lb:week:{iso}` ranks the week, `lb:solves:{date}` holds every solve of every day as `t|hints|tier|postId`, and `isoWeekRange()` turns a week key into its dates. Name, hook figure and period, reachable and retroactive for every week the boards have existed. It earns a menu item on the same grounds the funnel did: the numbers exist, but assembling them by hand means reading a 95-line audit dump and doing arithmetic correctly every week, and a moderator who has to work for a number will eventually publish a wrong one. It reports the week that actually has a winner and says which, because a moderator pressing this on a Monday should not be told "no solves" about a week that has not happened yet. **A human publishes** - this does not reopen auto-posting, which README records as paused after the automod bans |

### Boards that answer a question, and a way to see them (2026-08-17)

Owner's calls; rows 118-121 record them and what each one costs.

| # | Decision | Reason |
|---|---|---|
| 118 | **THIS CASE is a board, and it REPLACES TODAY rather than joining it.** `scope=case` reads `lb:{postId}`, ranked by time with hint counts; the rail is `This case / 7 cases / Streak / All-time`. Dec. 116's archive-aware TODAY tab is superseded and its machinery deleted | World ① had cells on the feed card and a line on the result sheet and **no board at all**, while the one time board on screen was scoped to the DAY. Those two coincide on every day except one that published twice - so a TODAY tab exists to serve the consequence of a missing double-press guard rather than any question a player has. The board itself was free: `lb:{postId}` has been written on every check since the leaderboard shipped, so it is correct retroactively for every case. Replacing rather than adding also keeps the rail at four tabs, which is what fits 360px. The day board is still SERVED - the mod funnel reads it - it is simply no longer a tab, which is why dec. 116 (teaching the TODAY tab to say the date on an archive case) is now dead code and was removed rather than left standing |
| 119 | **The week board is the LAST SEVEN CASES, computed at read time, and `lb:week:{iso}` stops being written.** `rollingWeek()` walks the last seven entries of `lt:posts`, maps each to its date, and re-prices each solve from the raw `lb:solves:{date}` record | A calendar week empties every Monday and means a different amount of play depending on how many cases went out in it; a rolling window is always full and always the same size, so two readings of it are comparable. The consequence is a feature rather than a cost: this board **cannot** be accumulated with `zIncrBy`, because "the last seven" changes membership on every publish - so it has to be computed, and a computed board cannot drift. `lb:applied` exists precisely to stop an accumulated board drifting from the days it is made of, and there is nothing here to drift. Points are recomputed per CASE rather than read from `lb:points:{date}`, because that key holds one figure per DAY and a day can hold two cases |
| 120 | **A case closed on the day it ran is worth `FRESH_BONUS = 250` more, added OUTSIDE the [300, 1000] clamp.** The flag is stored in the solve record (`t\|hints\|tier\|postId\|fresh`); a record written before the field existed counts as fresh | Owner: *«чтобы не решил все старые задачи и не выбрался в топ»*. The exploit is real and specific: a day holds one scored record per player, but its DATE is the case's, so ten archive cases closed in one sitting write ten records on ten dates. Written as a bonus for the daily and never as a cut to the archive, and the wording is load-bearing rather than diplomatic - the archive is how a newcomer catches up and how a streak survives a day with no new case, which `streak.ts` deliberately protects by letting ANY case extend a run. Taking points off the archive would contradict that at the same moment as this rewards the daily. **Outside the clamp on purpose**: inside it the bonus would be swallowed by the cap for exactly the players it is meant to reward - a fast solve already reaches 1000 on speed alone - so the rule would only have bitten slow solves. The daily range becomes 550-1250 and the archive keeps its original 300-1000. **Two consequences named rather than discovered.** The nightly rollup re-prices from raw records, so the formula applies retroactively on its first run; the migration default (missing flag = fresh) is what keeps that from demoting eight live players for a rule that did not exist when they played, so on prod **no existing standing moves** and only new archive solves are priced lower. And ALL-TIME becomes a long-service record: a newcomer cannot catch a veteran, because the veteran has more days, not more skill. For a daily game that is arguably correct, but it is a choice and it is now written down |
| 121 | **The streak board stays on `best`.** Not revisited | The rank a player holds and the flair beside their name are both `rankState(best, current)` - one function, deliberately, so the game and the subreddit cannot disagree about a title. Ranking the board on the current run would reintroduce exactly the split that was fixed (dec. 100), on the surface where it is most visible |

### Synthetic players, and the three guarantees that pay for them (2026-08-17)

| # | Decision | Reason |
|---|---|---|
| 122 | **Test players can be seeded on `deducto_puzzle_dev` and nowhere else.** Two mod-menu items (seed / remove) behind one guard in `src/server/seed.ts`; every synthetic name is prefixed `[test]`; every one is listed in `lt:seeded`; the audit prints them | The need is real - a leaderboard cannot be judged with one player on it - and so is the danger: fake names on a real board are precisely what dec. 37 forbids, and that rule is why the showcase's demo statistics went, why the sample histogram and demo leaderboard were deleted from the result screen, and why a guest is told plainly that nothing was recorded rather than shown an invented streak. Three guarantees are enforced in code rather than remembered. **It refuses anywhere but dev, loudly**: the check is `context.subredditName` against a hard-coded constant - not a config value, not an env var, not "is this not prod" - and a refusal names both the sub it was run on and the only one allowed, because a silent no-op is how somebody concludes the feature is broken and looks for a way around it. **It is reversible**: every write goes to a member of `lt:seeded` and the remover walks that same registry, so undo is exact rather than a guess. **It is marked in the data**: `[test]` cannot be a Reddit username (brackets are not legal), so a seeded row can never collide with a person and is recognisable by eye on any surface and in any export; `lt:seeded` and a `seeded: "1"` field on the profile are the second and third marks, for anything reading keys rather than pixels. It deliberately seeds only the aggregate boards - no `att:` rows, no `stats:` counters, no votes, no per-case boards - so nothing it writes can make a CASE look busier than it is |

### The streak counts CASES, not days (2026-08-17)

| # | Decision | Reason |
|---|---|---|
| 123 | **A streak is the number of published cases IN A ROW closed on the day the case ran.** The unit changes from calendar days to cases; `streak:cases:{userId}` records the links, the chain is derived against `lt:posts` on every write and its liveness on every read, and every surface that said "days" now says "cases" - including the rank ladder's rungs | Owner: *«надо решить каждый день по кейсу. не просто зайти. не просто подряд решить старые кейсы, а надо именно новые кейсы решать»*. The hole is real and the bank makes it cheap: 160 archive cases means one old case a day is a hundred days of "streak" without ever opening a daily, and a unit of DAYS cannot tell that apart from real attendance because the thing it measures - did you come back - is exactly what an archive grinder also does. **The argument this file was built on survives intact**: a hole in the feed still cannot break a run, because if the sub published nothing there is no case to have missed, so the chain neither advances nor breaks. The player is still never punished for what they do not control; what they are now measured on is the one thing they do. Three things had to be handled rather than discovered. **The current case is PENDING, not missed**, while its own day is still running - otherwise every publication would zero every player's streak until they got round to it, which is a run ended by someone else's button. **The feed is the registry UNIONED with the player's own closes**: `lt:posts` only started being written recently, so walking it alone would make a real, live close on an older post invisible; a case the player closed is a case that was published, whatever the registry remembers. And **the word had to move with the unit** (docs/11 R1): "3 days" became "3 cases" everywhere, because a figure whose unit changed silently is the same defect as a label over the wrong population |
| 124 | **`best` is a floor, and the zero the change causes is explained on screen rather than left to be found.** The new chain is seeded once from `lb:solves` records that name a case and say it was closed fresh; `best` is `max(new, stored)` as it always was; and `StreakState.legacyBest` drives one sentence on the result sheet and in the player's file | The unit change zeroes every live player's current run in one instant, for a reason no player did anything to cause. Two halves. **The record is untouched** - dec. 100 promises that a rank, which drives the subreddit flair, stays earned, so the new computation can only ever raise `best`, never lower it. **And the zero gets a sentence**: *"Your longest run was earned when a streak counted days you came back. It stays yours. A streak now counts cases closed on the day they run - so a new one starts with the next daily case, and the archive no longer adds to it."* Silence was the worst of the three options here: an unexplained zero reads as a bug, and the player who most deserves the explanation is the one who had the longest run. The seed recovers what the records can prove and no more - `lb:solves` holds one entry per player per DAY, so on a day that published two cases only the case owning that entry can be recovered, and a record written before the freshness flag existed decodes as fresh, the same forgiving default the points migration uses |

### The feed card, closed out (2026-08-18)

| # | Decision | Reason |
|---|---|---|
| 125 | **The twelve squares leave the unsolved card.** The docket before a close is now `CLUES` · `CLOSED IT` · `CURRENT STREAK`, and the board component is gone from this screen entirely | The owner met it three times, finally as *«почему когда я кейс не решил есть эти ебаные ячейки пустые»*, and the answer is that an empty progress bar is **the same picture on every reader's screen** - it cannot differ between readers, so it carries nothing. Dec. 95 had already made this exact argument about the fraction beside it and removed `0/12`; the graphic was simply never held to the same standard, and it was taking a third of the strip. What replaced it is the owner's own answer and all three are things a reader who has not begun can act on: how much evidence is in the file, how many people have already closed it, and the run of their own that is on the line tonight |
| 126 | **Your time is printed with the position it earned - `04:31 / 3RD OF 8` - and the position is counted by SCORE, not by name** | Owner, twice: *«показывать какое место на кейсе занял среди всех»*. It existed on the result sheet only, and for a structural reason: `lb:{postId}` is keyed by username, /api/preview renders on a feed scroll and is forbidden a Reddit call, so the card could not ask who it was talking to. Counting how many recorded times are strictly faster needs no identity at all - times are whole seconds, so everyone ahead of you sits in `[0, t-1]`. Two consequences taken deliberately: **equal times share a position**, because ordering them by who wrote first is a claim the data does not make; and the count is **clamped to the field**, because a solve whose username never resolved has no row and would otherwise be told `9th of 8`. It rides its own line under the figure - the collision dec. 98 deleted `of 7` for is the same one - which also makes the two case cells the same height |
| 127 | **`You` is red, and it arrives with one pulse.** `.tag.you` takes `--stamp`; `.tag.pb` deliberately does not | *«не хватает цвета»*, *«YOU - хочется выделять как-то лучше»*, three times, refused twice. "Colour belongs to the evidence, chrome is ink on paper" is the rule the palette is built on, and an ink-filled 9px box obeying it is precisely what the owner could not find on a board of twenty rows - the one thing anyone opens a leaderboard to look for. The rule is kept where it was earned and bent where it was costing the reader that: the standings screen spends no red at all, so nothing competes. `.tag.pb` is excluded because game.css reserves that screen's single red for the CLOSED stamp and a personal record does not outrank the verdict. The pulse is **one-shot, `both`, and off under `prefers-reduced-motion`** - a marker that says "here" once, not motion in a feed |
| 128 | **The two buttons are equal halves; the hierarchy rests on the fill alone.** Also: `.dk` keys step from `--ink-3` to `--ink-2`, and the case title from 600 to 700 | Owner, twice: *«хочу кнопки одинаковые сделать»*. Dec. 98 built the hierarchy out of width AND fill, sizing the standings button to its own label so the pair could not read as equals - and the result read as an afterthought bolted to the side of the real button rather than as the second half of a choice. One filled button beside one outlined button of the same size is still unambiguously a primary. The ink steps answer *«цифры сливаются»*: at 11px with .12em of tracking the muted-caption token was doing a label's job, and one step of ink stops the key dissolving while the figure keeps its own step above it |
| 129 | **The four items are printed as glyphs under the brief, and the case's hook replaces the rule of the game.** The rank the streak earned is printed under the streak | *«прикольно увидеть маленькие иконки предметов»* - they are the fastest thing on the card to read, no language and no figure, and together with the hook they are the only part of the card that is **different every day**, which is the complaint plans/07-variety.md exists for. The rank rides the streak because the owner asked to *«как-то с рейтингом связать»*, and it is the same word the subreddit flair puts beside the player's name, so the card and the comments agree rather than each keeping their own ladder. Absent below the first rung: a caption saying you have no rank yet is a line spent saying nothing (dec. 37) |
| 130 | **The offline harness was blessing a call it never performed: the Redis stub ignored `{ by: "score" }`** and read the range as rank indices | Found by writing the test for dec. 126 first and watching it fail with the field size. A score window of `[0, 299]` was returning the first 300 **rows** - on every board this app has, that is the whole board - so an implementation counting who is faster than you would have been handed the wrong answer and passed. This is the second time the offline gate has been the thing that was wrong (v0.0.7 shipped two bugs through a green suite), and the lesson is the same one: **a stub that answers a call it does not implement is worse than one that throws**, because the suite stays green while measuring nothing |

### The line under the title (2026-08-17)

| # | Decision | Reason |
|---|---|---|
| 125 | **Before you solve, the card carries the case's own HOOK instead of the rule of the game; after you solve, it carries the previous case's winner alongside the countdown.** `/api/preview` gains `case.legend` and `prev`; the hook is the legend's FIRST SENTENCE; the rule moves to `.tierline` on the wide layout, where there is room for both | Two things were already written and drawn nowhere. **`theme.legend`** has been in `src/shared/themes.ts` since the first commit and sent by `publicPuzzle` since the leaderboard shipped; the client declared it in `DailyResp` and never printed it. It is the only line on the card that differs from case to case - which is exactly the complaint behind `plans/07-variety.md` (internal): six themes exist and a player who never sees them has no way to know it. What it replaces is the rule of the game, which was identical on all 160 cases and duplicated the sentence printed under the board itself, where the rule is actually applied. **Only the first sentence**, and that is the part worth stating: every legend is two sentences, and the second describes the board's shape ("Four gardeners filed reports - each at their own time, coat and tool") - which the card already shows as twelve countable squares and the button already opens. Measured across all six themes the first sentence runs **35-98** characters against the rule's 78: shorter on five of them, and longer on one - `The Cursed Pizza`, whose legend puts the shape of the puzzle in its FIRST sentence rather than its second. That one wraps to two lines, which the probe and the 360x320 frame both confirm fits. It is not cut to a character budget on purpose: a sentence truncated mid-phrase reads worse than a second line, and the six legends are content someone wrote rather than a field to be trimmed. **`postData.epilogue`** has carried the previous case's winner since dec. 40 and appeared on the board only; for a solver it is the one line anywhere on that card about a PERSON, and a solver has no use for a hook to a case they have finished. With no previous case - the first case ever, or one nobody closed - the slot carries the next case alone: nothing is invented to fill it (dec. 37) and nothing is moved in to cover the gap, because one true sentence reads better than a true one plus a filler. The board's own epilogue changes from `Yesterday` to `Last case` in the same pass: the sub does not publish daily, so on 2026-08-04, 08-07 and 08-09 the word was simply false |

| 131 | **The control rail is four glyphs of one size, and the labelled buttons beside it are two labels of one size.** The standings button loses the label it grew above 720px; `.btn.lane` loses its private type scale | *«хочу кнопки одинаковые сделать. почему standings с надписью»* - and it was read against the feed card first, which was the wrong surface: the row the owner photographed is the in-game rail. Two separate drifts, one cause. The standings label was **load-bearing when the rail was the only way in** (*«до него хуй доберешься»*), and dec. 98 removed that reason by putting a full-size Standings button on the feed card, where a player who has not opened a case actually is - so above 720px the app was widening one button in a row of four for a reason that had already expired. `.lane` had the same shape of defect on the other end of the row: Warm-up and Hint sit side by side doing the same kind of thing and were two different sizes, because the metrics had been attached to a button instead of to the ROW. Both now belong to `.controls` |
| 132 | **The solved card's brief is three labelled facts, not a paragraph.** `LAST CASE` / `NEXT CASE` in the always-visible slot, `THE VOTE` with a five-square meter on the wide one; the figures that are the point of each row are set in full ink | *«можешь это красиво как-то оформить?»* The block read *"Last case: u/ma9leb in 03:18 · 1 solved it. Next case in 23h 56m. 1 of 5 votes in on how hard the next one should be."* - three unrelated facts run together, so a reader who wanted any ONE of them had to read all three, and the two figures the block exists for (a name and a countdown) sat mid-sentence with nothing marking them. A key column and inked figures is the grammar the docket beside it already uses, so the card now has **one** way of stating a fact instead of two. The vote becomes the app's own progress component - five squares, one per vote needed for a verdict - which had just come free when dec. 125 took it off the unsolved card; `1 of 5` is now legible without reading. The duplicated word goes too: `Harder 1 ... You voted Harder` became a lowercase tail on the tally |
| 133 | **`display` is never set on `.tierline` outside a media query** | Caught while writing dec. 132, not after shipping it. `.tierline` is the one element whose visibility is owned by breakpoints - hidden below 560px wide and below 300px tall - and a `.rows` modifier setting `display: block` outranks both, which would have pushed the vote row onto exactly the 320px phone slot the entire layout is budgeted against. The modifier lifts the clamp only. Stated here because the trap is structural: any future state class on that element has the same failure mode |
| 134 | **Automatic daily publication returns, and everything that makes it survivable ships with it** (`src/server/daily.ts`): a per-subreddit opt-in defaulting to OFF, an `hSetNX` day lock, a 20-hour floor, an hourly cron whose publishing hour is a SETTING, and - the one that matters - a job 30 minutes after each post that reads it back and **stops the schedule and modmails the moderators** if it was filtered | The first attempt got two subreddits banned, and the post-mortem found the scheduler was the least of it. Reddit's antispam does not act at submit time - it acts minutes later, and `submitCustomPost` returns a good post id either way - so **the previous version had no way of learning its posts were being removed until the subreddit was gone**. A daily poster without that loop is the thing that was shipped last time, and it is the reason this one is allowed to exist. The opt-in is what fixes the other technical cause: one `scheduler` block fires into EVERY install, so the old cron hit both subreddits in the same minute with the same title - which is the "identical text, several places, short window" pattern antispam looks for first |
| 135 | **The record lives in a comment on the post, and the feed card stops carrying the previous case at all** (`src/server/herald.ts`) | Owner: *«а на самом посте бы обновлял fastest time. а коммент будет как память»*. `FASTEST` is a live minimum over `lb:{postId}`, so the moment somebody is beaten their name is gone from every screen in the app and **nothing anywhere says they ever held it**. The card's `PREVIOUS CASE` row was not a fix for that - it was a live re-read of a DIFFERENT board, i.e. a row that said "winner" and showed "current leader", which is exactly why the owner read it as *«что это значит, я не понимаю»*. So the case closes 24 h after publication and its winner is named in a comment; a time that beats the record after that gets a comment of its own. **The mention is the notification** - naming `u/x` triggers Reddit's own username notification, so nobody needs a private message from an app account, which is both more intrusive and closer to the behaviour that was filtered before. Guards, because this is again an automated write to Reddit: one closing comment per case ever (`hSetNX`), records only after the close, only strictly better times, re-read at send time rather than trusted from the caller, and never twice inside six hours |
| 136 | **The screenshot harness was verifying the wrong screens, and the invocation was the reason** | `SPLASH` and `LEDGER` are lists of SCENE NAMES, not switches: `SPLASH=1` shoots one scene called `1` - which does not exist - while `LEDGER` keeps all 23 of its own. Three runs of ~25 minutes each therefore photographed the standings and the result sheet while reporting PASS about a feed card they barely touched. Compounding it, `${LEDGER:-…}` substitutes the default on an EMPTY value too, so `LEDGER=""` silently restored the full list, and the 320px slot widths were hardcoded, so changing one screen still cost the whole matrix. Now: `${VAR-…}`, a `SLOTS` variable, and a worked example in the README. The same change scoped a feed-card run from 210 frames / 25 min to **70 frames / 4.5 min**. Stated as a decision because the failure is the harness's third of the week and always the same shape - **a check that runs is not the same as a check that looks at the thing you changed** |
| 137 | **The fixture's payloads are typed against the client's own `PreviewResp`, and `npm run check:fixture` runs before a single frame is taken** | Three times in two days the harness fixture fell behind `/api/preview` and the run reported clean frames of a card missing the very element under test: `clueCount` rendered as `undefined`, then `caseRank` and `rank` never drew at all - so `YOUR PLACE` and the rank plate were absent from every "PASS" frame of the run that was supposed to verify them. Structural typing turns each of those into a compile error. Verified by positive control: removing `caseRank` from one fixture state fails the check by name, restoring it passes - because a check nobody has watched fail is an assumption |
### The phone card: three entities, and a budget that had to be paid for (2026-08-20)

| # | Decision | Reason |
|---|---|---|
| 138 | **Each item glyph gets the word for it, in four equal centred tracks** | Four emoji in a `space-between` row read as decoration pushed to the card's edges - *«отцентрировать сами картинки и подписи, блок не должен быть визуально сбитым»*. And at 21px several of them are simply not guessable: a kitchen timer reads as a clock, a wheat sheaf as a houseplant. The word is what turns the row into the evidence list it is, and four `1fr` tracks are symmetric by construction rather than by luck. The glyph then gives up a size step - it is a mark, the word is the content |
| 139 | **`CASES CLOSED` is gone; the cell carries the run, the rank it earned, and the next rung.** `CURRENT STREAK / 8 cases / 🔎 Inspector / 2 more cases to Detective` | Owner: *«CLOSED IT и CASES CLOSED визуально конфликтуют и создают ощущение повторения»* - two headings, both counting closures, neither saying whose. What replaced it is the only figure on the card a reader can change tonight, and the last line is the whole point of the block: *«задача блока - не просто показывать статистику, а мотивировать»*. `rankIn` counts from the run IN PROGRESS, not from `best` - the number printed has to be cases this player must still close, not a distance from a record they are no longer standing on. Absent at the top of the ladder rather than invented, and absent for a player with no run, who has nothing to protect |
| 140 | **The rank plate leaves the corner it was added to** | It was put there one version earlier because the corner was empty. The moment the player's own cell started naming the rank as part of a run and a target, the plate became the same word printed twice - and *«отсутствие визуального дублирования заголовков»* is in the same list. The corner belongs to the CASE; who the reader is belongs beside the figure that earned it |
| 141 | **On a phone the docket stops being a three-column table: two cells about the case share a row, the player's cell takes the full width under them and centres its lines** | Three cells inside ~336px gave each ~105px and the widest key on the card lives in the third, so it wrapped 2 + 1 with a hairline through the middle of nothing - *«левый нижний блок визуально слишком тяжёлый»*, *«лучше пожертвовать жёсткой табличной структурой»*. The horizontal rule across the full width is also a **stronger** world boundary (docs/11-stats-ia.md R3) than the vertical hairline ever was, and it is the break that stops `CLOSED IT` and the player's own count reading as one fact twice |
| 142 | **The 320px slot cannot carry the hook AND the named items AND a two-row docket, so at that height the items row IS the description** | Measured, not assumed: the probe put `.brief` at 90px of content in a 70px box, and in the playing state at 76px in 37px - the hook painting through into the docket's keys. Four named objects say what the case is about more concretely than a sentence clipped to two lines, they differ every day, and unlike the sentence they cannot be half-read. The hide is `.rules:not(.rows)`, because the same element carries the solved card's countdown and live-case link, which are short and have no items row competing with them. The rest of the gap was paid in measured pixels rather than guesses - band padding 3→2px, the CTA 50→46px (still over the 44px tap floor), the hook's bottom gutter 12→8px because the row that follows it belongs to it |
| 143 | **A card that stops growing at 420px has the same budget as the 320px slot, and the probe proved it** | The same overflow turned up at 360x800 as in the feed slot, because `.card { max-height: 420px }` caps the card on any tall viewport. So the items row's metrics are global rather than tucked inside a `max-height` query - a compaction that only applies in the slot is a fix for one of the two places the bug lives |
### The layout check went from 25 minutes to one (2026-08-20)

| # | Decision | Reason |
|---|---|---|
| 144 | **The whole matrix is measured from ONE browser launch per theme, in iframes sized to the slot** (`npm run check:layout`) | Owner: *«это не реально как долго ты проверяешь… максимум до 1-2 минут, нужен другой способ»* - and the diagnosis was mechanical, not a matter of shooting less. The old loop started a FULL Firefox process per frame: process start, profile load, page load, screenshot, exit, about seven seconds each, of which the measurement is milliseconds. **The verdict never came from the PNGs.** It comes from the DOM probe, which already posts a report that identifies itself - `location.search`, `innerWidth`, `innerHeight`. Inside an iframe those three ARE the scene and the exact box it was given, so an iframe sized to the feed slot is not an approximation of that frame: it is that frame, with the same media queries resolving against the same viewport. 448 measurements, both font profiles, both themes: **59 seconds**. `shoot.sh` stays for when a human wants to look at the frames |
| 145 | **A count is not coverage.** `coverage.py` asserts the SET of scene x size pairs, not how many reports arrived | The first version reported `113 of 112` and would have passed a counting gate. One scene reporting twice and another not at all is the same number and a completely different run - and on its first real outing this check caught a launch that measured **0 of 112** while the counter read full. Twice more it was the thing that failed a run everything else called green |
| 146 | **The browser dies BEFORE the counter is reset, not after** | The bug behind that 0-of-112, and it is an ordering bug rather than a timing one. The report counter is global; resetting it while the previous browser is still alive lets its last frames post into the new batch, so the poll sees a full count immediately, breaks, and kills the new browser before it has rendered a single iframe. A run that measured nothing and reported 112. The stray `113` was the same race, one frame wide, and it was written off as harmless - which is what a race looks like right up until it is not |
| 147 | **A hundred simultaneous iframes is an OOM kill; the grid loads in batches** | Exit 137, twice, before the cause was obvious. Sixteen live documents at a time costs nothing in wall clock, because each probe fires 1.1 s after its own frame loads and a batch therefore settles in the same 1.4 s whatever its size |
| 148 | **`pkill -f <pattern>` matches the command line of the shell running it** | Stated here because it cost most of an afternoon and reads as the tool hanging: a command containing `pkill -f "firefox --headless"` kills its own invoking shell, which surfaces as exit 137/144 and a session that appears to freeze. `shoot.sh` documents the same trap for the same reason. Process kills belong inside a script file, never in an inline command that names the pattern |
| 149 | **Three mod-menu items removed at the owner's word (2026-08-22): "the week's numbers", "seed test players", "remove test players".** `/internal/menu/week`, `/internal/menu/seed`, `/internal/menu/unseed` and `src/server/seed.ts` are gone; `isoWeekRange()` went with them as its only caller | The owner said plainly they do not use them. The weekly report (dec. 117) never became a weekly post, and the seeding pair (dec. 122) had done its job - the boards were judged, the feature kept only risk. Removal is the honest form of "unused": a menu that lists nine items where six are pressed is a menu nobody reads. What survives is the safety half of dec. 122: seeded rows already written are still marked (`[test]` prefix, `lt:seeded` registry), and the audit still names any it finds - the detector outlives the seeder because leftover data outlives the code that wrote it. If test players are ever needed again, dec. 122 records the three guarantees any reimplementation must pay |

### The clue teaches the glyph its name (2026-08-22)

| # | Decision | Reason |
|---|---|---|
| 150 | **Every item named in a clue carries its glyph beside the word** (`John didn't carry the 🌾 flour.`), and the ? help gains the case's four items, glyph with word (`itemRef()` in [render.ts](../src/shared/render.ts), `helpText()` in [main.ts](../src/client/main.ts)) | First outside player: *"the icons are too small and I struggled to figure out what the 'flour' referred to, as there seemed to be... a pie? and some grains?"* The defect is a broken mapping, stated once and never again: the board's chips are glyph-only (a 4-track cell has no room for a word), the clue was word-only, and the one place that paired them was the splash legend (dec. 138) plus a `title` nobody hovers on a phone - so mid-case, exactly where the question arises, neither surface answers it. Three repairs were on the table and two were rejected: glyphs INSTEAD of words in clues reverses the problem (now the word is unknowable) and turns prose into a rebus; a pre-case legend alone is dec. 138, which this player saw and did not retain - four arbitrary pairs don't survive to the board. Pairing them IN the clue teaches the mapping at the moment of need, every time, at zero taps. It is the dec. 63 line again and it lands on the content side: the glyph rides the puzzle's noun, not the chrome - and the hint panel (`fmtHint`) and solution table have paired glyph and word all along, so the clue list was the odd one out. Coats and times are untouched: their chip marks (a colour + letter, `09`) already contain the clue's word |
### The STREAK tab ranks the run still going (2026-09-08)

| # | Decision | Reason |
|---|---|---|
| 151 | **The STREAK board ranks the CURRENT run, not the all-time record.** `lb:streakcur` is mirrored by `recordVisit`; the board itself is computed at read time | Owner: *«сейчас показывается лучшие стрики за все время, но я думаю надо текущий показывать»*. A table of records is a monument - nobody can move it today, so nobody reads it twice; the run a reader can still lose tonight is the one worth ranking. `lb:streakbest` is untouched: the rank ladder and the subreddit flair read it, and a record stays earned (dec. 100) |
| 152 | **Liveness is decided against the newest case whose DAY IS OVER, not against the newest case** (`registryFront`) | The rule it replaces asked only whether the NEWEST case had been missed - which says nothing about a case that published, went by unclosed, and was superseded between two of the player's own solves. **A run broken on Monday came back to life on Wednesday the moment Wednesday's case went out**, because "the newest case is still today's" was read as "nothing was missed", and it stayed back until midnight. It oscillated daily: dropped every morning, resurrected at every publication. Found by adversarial review and reproduced through the real routes; the defect predates the tab change and had been lying on the player's own card, where nothing compared it to anything |
| 153 | **One definition of "pending", shared by the run's arithmetic, the liveness check and the audit** | The first cut of dec. 152 moved only the READ path onto `registryFront` and left `recordVisit` deriving a single pending case from `lt:lastPostDay` - the pointer the same file had just declared unfit for the question. On a day that published two cases the morning one was then walked as decided-and-missed, so a player who closed the EVENING case watched a run of four collapse to one, and that one was written to the hash and published on the board. The mirror image of the bug being fixed, introduced by fixing half of it. `runsOfCases` now takes the day's pending SET, and `audit.ts` - which held a third copy of the rule and would have confirmed the wrong number rather than catching it - reads the same helper |
| 154 | **The viewer's own row works on a computed board** | `key` is `""` for a computed board, so `zScore(key, viewer)` silently answered undefined and the `you` row never rendered. Invisible while the rolling week was the only computed board; it would have shipped on the current-streak board, which is the one screen a player opens to find exactly their own line |
### Deducto is a collection of daily logic formats (2026-09-16)

| # | Decision | Reason |
|---|---|---|
| 155 | **The product is the daily ritual; the mechanic is a variable.** Deducto is repositioned from "a daily deduction-grid game" to "daily logic games" - one puzzle a day, format varying. The Deduction Grid becomes one format among several; **Kings & Jacks** (truth-tellers and liars) is designed next | Owner's call. The thing that brings a player back is *today's puzzle*, not the grid - and the grid alone has a ceiling the project has already met once: the first external player left because it was the same task again and again (the variety ceiling, still open in `plans/07-variety.md`). Varying the mechanic attacks that ceiling where new themes and new difficulties cannot, because a theme is a skin and a format is a different kind of thinking. The subreddit is positioned the same way: the place you drop into once a day to exercise your logic, not the place that hosts one puzzle type |
| 156 | **The streak counts PUZZLES, so a change of format never costs a run** | This is the premise, not a convenience. A streak that reset when the format changed would punish the player for the variety the collection exists to provide, and the first thing anyone would learn is to hope the format does not change. It also happens to be free: the streak was already rebuilt to count published cases against a registry of posts (dec. 123) and has never inspected a puzzle |
| 157 | **One new field on a published post - `gameType` - and a rule that it must not leak upward** ([12-game-types.md](12-game-types.md)) | The seam is cheap now and expensive later: it is paid once, and not having it is paid at every format after the second. The rule that matters is not the field but its blast radius - the server resolves the type once at the edge, and a `switch (gameType)` in the leaderboard, the streak or the result sheet is the seam having failed. Absent reads as `"grid"`, so every post published before the field existed keeps working and no migration is needed. **Not implemented**; written down before the second format is built, deliberately |
| 158 | **Four grid-shaped assumptions above the line are named now rather than discovered per-format**: the vocabulary (*case*, *suspect*, *detective*, *clue*), the feed card's size figure (`CLUES 10`), difficulty (the tier ladder and the Harder/Same/Softer vote are defined against the grid's generator), and measurement | Measurement is the one with a deadline. The concept asks for daily players, completion rate, solve time, D1/D7 and streak distribution **per format** - and every one of those is currently aggregated over the one format there is, which reads identically to "aggregated over all of them" and will keep reading that way, silently, after the second arrives. The format has to be stamped on the day's record when it is written, because it cannot be recovered afterwards |
| 159 | **No paywalls, no premium puzzles, no gated formats** - only an unobtrusive *Support this app* link, unbuilt | A daily habit is worth more than a conversion, and a gate is the fastest way to end one. Recorded so that a later "just this one format" cannot happen without contradicting a written decision |
### The bank grows by batch (2026-09-25)

| # | Decision | Reason |
|---|---|---|
| 160 | **100 more 🟢 appended as batch 2: the bank is now 15 tutorial / 145 🟢 / 100 🟡 = 260** | The daily default is green (dec. 42), the bank held 45 greens, and case #56 had gone out - so the green bucket had already wrapped and the sub was being served repeats. Yellow is served only when the sub votes Harder and was never the resource running out. The new cases are indistinguishable in profile from the old: median difficulty score 29 vs 28, median 10 clues vs 10, relational share 60% vs 59%, the same clue-type mix - every one unique in clues and in answer, every one solvable on the board 12/12 |
| 161 | **Appended, never rebuilt: `build-bank.ts --append` copies every existing entry verbatim, including its `score`** | A live post stores its bank index, so a rebuild that moved anything would swap the puzzle under an open post. The engine was checked first and DOES reproduce the old entries byte for byte across every tier - but a full rebuild also recomputes `score`, which orders the warm-up pool under players whose per-user counter points into it. Growing the bank should not be able to touch a single byte of what is published, and append is the mode where that is true by construction. Verified after the build: 160 old entries, 0 changed |
| 162 | **Each batch is spread by theme on its own, and the batches are served end to end** (`bucketFor`) | Appending to ONE pool would have reshuffled it - `spreadByTheme`'s greedy pick depends on how many cases each theme has left - and the persisted cursor would then point into a different sequence: a case played last month could come straight back, and new ones be skipped for a whole lap. Batch by batch, the original order stays exactly where the cursor left it and the new cases come after it; a cursor that had already wrapped lands straight in the new batch. The route test was watched failing against the one-pool version before it was trusted |
| 163 | **Never the same case twice: the published set is recorded (`lt:served`), seeded once from the posts themselves, and every pick skips it** | Owner: *«повторы надо удалить, чтобы больше не было»*. The old pick was `bucket[cursor % length]` - a counter going round a loop, which cannot know what was played; with 45 greens and case #56 out, the loop had already come round. Reconstructing the played set from the counters would have been a guess, because the bucket order changed twice during the project (the tutorial append, then the theme spread), so the seed reads the truth instead: every Deducto post carries its bank index in `postData`, and one listing of the subreddit recovers them all. The pick claims a case with `hSetNX`, so two publishes racing for one case cannot both win it; only when EVERY case of a level has gone out does it repeat, and then it says so in the log. Verified by positive control: without the seed, and separately without the claim, a published history case comes back |
| 164 | **Batch 2 was rebuilt HARDER before it ever reached production: no easy band, three in five from the hard band** | *«я бы добавил посложнее ... потруднее бы побольше»*. Median difficulty score 35 against the played greens' 28, median 9 clues against 10, relational clues 70% against 59%. Still 🟢 - every case solvable on the board, 12/12 - so "harder" means fewer and more relational load-bearing clues, not a board that stops helping (the line dec. 42 drew). Rebuilding was safe only because production had never seen indices 160+; the 160 published entries are byte-identical, checked again after the rebuild |
| 165 | **A random hard day, about one in seven, when the vote decides nothing - never in an install's first week** | *«люди мало голосуют ... пусть иногда случайно попадаются сложная»*. The dice are a hash of the CASE NUMBER rather than `Math.random`, because the feed card and the result sheet announce tomorrow's level: keyed on the number, the server knows today what tomorrow will be, and the forecast is right every time (checked over 70 publications). A hard day is one-off - the persisted level stays at the default - and a decided vote is always followed. `moved` stays the vote's word: dice are not the sub having moved anything, and the publish toast now says *"a random hard day - nobody voted"* instead of crediting the vote. The first week is excluded because the unguarded dice put case #1 on a hard day, i.e. a newcomer's first case in hard mode |
| 166 | **The warm-up lane is ONE case, the easiest, on a clean board every round** | *«на разминку одну и ту же я бы оставил»*. The lane teaches a mechanic, and a mechanic is learned by doing the same thing until it is obvious. Every round still opens clean because a practice slot is keyed by the player's own round counter, not by the case. The other 14 tutorial cases stay in the bank: deleting them would shift every index after them, and published posts store those indices |
| 167 | **A per-case record is written to the app log once a day, and the no-repeat set is seeded by the same hourly job** (`case-stats`) | The question *"how were the last 50 cases solved"* had no answer in one place: solves are keyed by post, difficulty by bank index, and nothing joined them. The listing that seeds the no-repeat set also yields each post's index, so one job produces players, times, hints, the funnel and the offline difficulty score side by side - readable with `devvit logs` and no button, which is the rule the owner set for statistics |

Note. Mentions of the view switcher and the tabbed variant (dec. 14, 17, 23) and the "playtest switcher below the post" (dec. 23-24) are **historical**, overridden by decision 29 (B): a single view (board), #viewswitch/devbar removed. The compact grid itself from dec. 24 remains - that is the board, resized by dec. 47.

Note (2026-08-07). Decisions **1** and **29** (single board, no tabs) are re-confirmed, not revoked: the value×value layer that would need tabs is deferred (plan 03, C3) because the daily default drops to 🟢, where the board can express everything the case needs.

Open question: on touch there's no "clue ↔ board cells" link (highlight removed in dec. 10 as confusing). If mobile playtests show that players lose the link, - return to a tap-toggle highlight, not hover.

## Methodology (learned from a mistake)

- Change **one variable per iteration** - changing four aspects of the interaction at once made the behavior inexplicable.
- Frame contentious levels of automation as **toggles in the prototype**, to compare them live in one session.
- Interaction acceptance criterion: the player finishes a game without once asking "why did that happen?".
